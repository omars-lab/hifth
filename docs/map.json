{
  "$comment": [
    "The map: what this app is made of, and where you touch it to change each part.",
    "",
    "Same rule as docs/validation/ledger.json — one source, several renderers. This file",
    "is the only place the map is written; `make map` prints it, `make map FEATURE=<id>`",
    "prints one feature, and the /extend skill drives a session from it. Never restate a",
    "feature's entrypoints anywhere else; point at the id.",
    "",
    "WHY THIS IS JSON AND NOT PROSE, and why it holds symbols instead of line numbers:",
    "an orientation doc is only worth reading if it is true, and the usual ones rot",
    "silently — a file gets renamed, a function moves, and the map keeps confidently",
    "pointing at nothing. scripts/gate-map.mjs runs in CI and in the pre-commit hook: it",
    "opens every file listed here and checks that every `symbol` still literally appears",
    "in it. A rename fails the build on the commit that does the renaming, naming the map",
    "entry to update. Line numbers are what rots fastest, so they are never stored — the",
    "renderer computes them at print time, so what you read is always where it is now.",
    "",
    "FIELDS",
    "  id         kebab-case, stable; what you pass to `make map FEATURE=`.",
    "  what       one line, in the user's terms, not the code's.",
    "  layer      L1 core | L2 web | L3 etl | gate — where this feature's weight sits.",
    "  entry      the ordered path through the code. Read them top to bottom and you have",
    "             the feature. `file` + `symbol` (checked) + `note` (why you'd stop here).",
    "  extend     ordered steps for the most common change to this feature. Written as",
    "             what to do, not what exists.",
    "  gates      what will judge the change — run these before you believe it works.",
    "  seeAlso    ids of features that will notice if you change this one.",
    "",
    "ADDING A FEATURE: add an entry here. The gate does not require one per file, and it",
    "should not — a map that lists everything is an `ls`. List what someone arriving",
    "cold would otherwise have to find by grep."
  ],

  "features": [
    {
      "id": "select-an-ayah",
      "what": "Tap an ayah on the page, or drag a marquee across several, and see amber marker swipes laid over them.",
      "layer": "L1 core",
      "entry": [
        {
          "file": "packages/core/src/gestures.ts",
          "symbol": "pointerIntent",
          "note": "The one decision the whole surface rests on: is this pointer a pan, a pinch, a tap, or a marquee? Everything downstream is a consequence. TAP_SLOP_PX and LONG_PRESS_MS are the thresholds a finger is judged by."
        },
        {
          "file": "packages/core/src/resolver.ts",
          "symbol": "class Resolver",
          "note": "Screen point → ayah key, via the polygon manifest. The adapter between geometry and scripture; nothing above this layer knows what a polygon is."
        },
        {
          "file": "packages/core/src/highlighter.ts",
          "symbol": "class Highlighter",
          "note": "Owns #hifth-overlay and every mark drawn in it. Framework-free and imperative on purpose — React must not re-render the stage. If you are adding a new kind of highlight, it is a StyleToken here, not a component. `paint` is where a style becomes either marker swipes or a cloned shape."
        },
        {
          "file": "packages/core/src/ink.ts",
          "symbol": "swipesFromPath",
          "note": "Why the highlight looks like a pen and not a box. Every ayah's hit area is a run of axis-aligned rectangles, one per line it occupies; this turns each into a band along the line's middle. It is a parser with a fallback, not an assumption — it returns null on any geometry it does not fully recognise and the Highlighter clones the shape instead, so an unfamiliar page degrades to the old box rather than to nothing. Loop 4b was the test of that: 601 pages arrived at once from the same upstream, and the fallback is still there for the edition that does not come from it."
        },
        {
          "file": "packages/core/src/highlighter.ts",
          "symbol": "private paint",
          "note": "The swipe branch is also the whole contract with the wipe animation, and none of it is visible. A `<line>` renders identically whichever end you write first, so the order of x1/x2 here is the only thing deciding which way the ink travels — right to left, because that is where a pen meets Arabic, and a backwards wipe would look subtly wrong to a reader and correct to everyone else. `--hl-len` and `--hl-i` are the two numbers CSS cannot work out for itself: a dash pattern must be told the stroke's length (`100%` on a line is the viewport, not the line) and which line of the ayah this is, so line 2 starts after line 1."
        },
        {
          "file": "apps/web/src/components/PageStage.tsx",
          "symbol": "PageStage",
          "note": "The React boundary. Binds @use-gesture to the surface, hands events to pointerIntent, and calls the Highlighter imperatively. The only file where the two worlds touch."
        }
      ],
      "extend": [
        "Add the intent to `PointerIntent` and teach `pointerIntent`/`nextIntent` when to return it — with a unit test in gestures.test.ts first, because this is pure and cheap to pin.",
        "If it draws something new, add a `StyleToken` and its branch in the Highlighter, then a CSS custom property in apps/web/src/styles/tokens.css. Decide whether it is ink: if so add it to `INKED` and give it an `--ink-*` token, because the ink rules blend with `multiply` and the alpha washes are tuned for compositing over — the two families are not interchangeable.",
        "Wire it in PageStage. Resist adding state to React that the Highlighter already owns.",
        "If it is inked it inherits the wipe, so it must emit `--hl-len` and `--hl-i` per element or it will draw solid and unanimated — the deliberate fallback, not a bug, but not the pen either. A motion rule that also sets the `animation` shorthand has to say which of the two renderings it is for: `.hl-sel.pulse` outranks `.hl-ink`, which is why the pulse carries `:not(.hl-ink)`.",
        "Add a golden row if it changes pixels — see the `golden-images` feature. Note that a wipe is not pixels: Playwright fast-forwards finite animations to completion, so the goldens see the finished stroke and cannot see the wipe at all. Anything about how a mark arrives has to be a unit test on what the Highlighter wrote."
      ],
      "gates": ["pnpm test", "make e2e", "make golden"],
      "seeAlso": ["the-hop", "root-lens", "word-selection", "golden-images"]
    },

    {
      "id": "the-hop",
      "what": "From a selected ayah, jump to the mutashabihat that resemble it — the reason this app exists.",
      "layer": "L1 core",
      "entry": [
        {
          "file": "packages/core/src/adjacency.ts",
          "symbol": "class Adjacency",
          "note": "Loads and caches the per-surah edge shards from public/assets/adj/. Start here to understand what an edge IS."
        },
        {
          "file": "packages/core/src/adjacency.ts",
          "symbol": "bucketEdges",
          "note": "Edges → rail chips, grouped by direction. `orderForHifz` beside it is the ranking a memoriser actually wants, which is not the ranking the data arrives in."
        },
        {
          "file": "packages/core/src/adjacency.ts",
          "symbol": "refineByWords",
          "note": "What a word run does to the edge list, and the reason it returns THREE outcomes rather than two. `about` is the edges whose span overlaps the run; `excluded` counts the ones that name words elsewhere in the ayah; `unplaced` is the edges that name no words at all — 15% of them, because a span is only emitted when the longest shared run occurs in exactly one place on both sides. Folding `unplaced` into either of the others would be a claim the data does not make. `Adjacency.hopsForWords` is the same thing keyed by a `<ayah>#w3-7` run, and it holds no root shards, so a caller who wants shared-root edges judged must pass `options.roots` — see `root-lens`."
        },
        {
          "file": "apps/web/src/components/HopRail.tsx",
          "symbol": "HopRail",
          "note": "The chips down the side. Arabic-Indic digits throughout, including in aria-labels — a sighted reader seeing ٣ and a screen-reader user hearing 'three' is a defect, and one the aria snapshots caught."
        },
        {
          "file": "apps/web/src/components/HopPopover.tsx",
          "symbol": "HopPopover",
          "note": "The chip's expansion: the destinations, and the diff against the current ayah."
        },
        {
          "file": "apps/web/src/components/PageStage.tsx",
          "symbol": "async navigateTo",
          "note": "The cross-page jump itself, and the case that is easy to get wrong: the destination is on another page, so the page must mount before the wash can land. `navigatedRef` is what stops an in-flight fetch from stomping a deep link that already arrived."
        },
        {
          "file": "packages/core/src/view.ts",
          "symbol": "frameBboxToView",
          "note": "Where the hop lands. Scale SVG user units to px, put the target bbox's centre on the stage's centre — then `clampView` beside it decides whether the page may actually go there. Centring is a request, not a right: an ayah 40 px from the foot of the page can only be centred by dragging the page half a screen past its own end, and the reader is then looking at a margin. `holdAxis` is the whole rule in two regimes — a page bigger than the stage may roam over its overhang and no further; a page smaller than it has exactly one honest position, the middle."
        },
        {
          "file": "apps/web/src/components/PageStage.tsx",
          "symbol": "const applyTransform",
          "note": "The single choke point every hop, tween, drag and pinch writes through, which is why the clamp lives here and not on the hop's target: a lerp between two legal views passes through a zoom belonging to neither, and a drag after a correct landing would otherwise shove the page into the void. `measureFit` caches the stage/page box per gesture rather than per frame — reading layout inside a RAF tween is a read straight after a write to the element being measured, the forced-synchronous-layout shape the perf budget exists to keep out."
        },
        {
          "file": "apps/web/src/components/TrailBeads.tsx",
          "symbol": "TrailBeads",
          "note": "Where you have been. The hop is only useful if you can get back."
        }
      ],
      "extend": [
        "New edge kind: add it to `EDGE_TYPES` in adjacency.ts and give it a status. `isActiveEdgeType` is what keeps a provisional kind out of the UI while its data is still being audited.",
        "Emit it from packages/etl/scripts/build-adjacency.mjs, then re-run `make etl` — CI diffs the committed shards against a fresh ETL run, so a non-deterministic change fails the build.",
        "If a human has ruled on specific edges, they belong in the curated set, not the dataset — gate:verified-edges enforces that a 'correct' edge still ships and a 'wrong' one stays gone, forever.",
        "Check the shard budget in the ETL output; the largest shard is printed on every run.",
        "Changing where a hop lands is `frameBboxToView`, and any *new* write to the host transform must go through `applyTransform` — a path that sets `host.style.transform` itself is a path the clamp cannot see. apps/web/e2e/stage-fit.spec.ts is what notices: it measures the page against the stage, which no golden shot can do because the crop is the SVG element and the defect is the space around it. Note which box a new handler measures its origin against — `view.x/y` are *layer*-relative, and reading a rect off the stage instead is the §7 ⑨ defect, invisible at rest because the anchor error is proportional to `(1 − zoom ratio)`. The last test in that file drives a real zoom and follows a point through the SVG's own user space, which is the only frame a transform does not move."
      ],
      "gates": ["pnpm test", "pnpm gate:verified-edges", "make ci", "make e2e"],
      "seeAlso": ["edge-data-etl", "share-links", "root-lens"]
    },

    {
      "id": "root-lens",
      "what": "Show every ayah sharing an Arabic root with this one — the other way ayahs rhyme.",
      "layer": "L1 core",
      "entry": [
        {
          "file": "packages/core/src/roots.ts",
          "symbol": "class Roots",
          "note": "Two shard families: per-ayah refs and per-root buckets. The split is what keeps a 44k-pair index inside the byte budget on a phone."
        },
        {
          "file": "packages/core/src/roots.ts",
          "symbol": "orderByPageDistance",
          "note": "Ranking for someone holding a mushaf: near pages first, because that is how the memory is organised."
        },
        {
          "file": "packages/core/src/roots.ts",
          "symbol": "rootsForWords",
          "note": "Which roots a word run actually carries — the word-granular half word-D added, and the seam between the two lenses. Every per-ayah root ref now names the PRINT word indices it sits at (`w`), baked in by the ETL, so this is a filter and not a second alignment. `Adjacency` has no root shards, so this is what a caller hands it as `options.roots` before asking `hopsForWords`; skip it and every shared-root edge is reported as unplaced rather than matched."
        },
        {
          "file": "apps/web/src/components/RootLens.tsx",
          "symbol": "RootLens",
          "note": "The ⬡ surface. Note the glyph is shared with a rail chip and the two promise different things — that collision was a decided defect, not an accident."
        }
      ],
      "extend": [
        "Roots are derived, never hand-edited: change packages/etl/scripts/build-roots.mjs and re-run `make etl`.",
        "Granularity is ayah-level in the UI and word-level in the data since word-D: each per-ayah ref carries `w`, the print word indices the root occupies, which is what `rootsForWords` reads. The ⬡ surface has not been narrowed to a run yet — that is a UI question, no longer a data one.",
        "The `w` indices are the PRINT's, not the Quranic Arabic Corpus's, even though the roots themselves come from QAC. The ETL applies the alignment and ships the answer; nothing in L1 or L2 knows a second index exists, and re-introducing one here would undo placement C — see `word-indexing`."
      ],
      "gates": ["pnpm test", "make ci"],
      "seeAlso": ["edge-data-etl", "the-hop"]
    },

    {
      "id": "share-links",
      "what": "Every view has a URL, and every URL restores its view — offline, from a cold start.",
      "layer": "L1 core",
      "entry": [
        {
          "file": "packages/core/src/router.ts",
          "symbol": "serializeState",
          "note": "State → hash. Its inverse `parseHash` must round-trip it; that pair is the whole contract and it is unit-tested as a pair."
        },
        {
          "file": "packages/core/src/router.ts",
          "symbol": "parseHash",
          "note": "Hash → state, and the place to be forgiving: a hand-typed or truncated link should degrade to something sensible rather than a blank page."
        },
        {
          "file": "apps/web/src/useHashRouter.ts",
          "symbol": "useHashRouter",
          "note": "The React binding. Hash routing, not history routing, because this ships as static files with no server to rewrite paths."
        },
        {
          "file": "apps/web/src/components/ShareSheet.tsx",
          "symbol": "ShareSheet",
          "note": "Where a link leaves the app. Do not reach for the Clipboard API without a fallback — a LAN preview is plain http, so it is not a secure context and the API is simply absent."
        },
        {
          "file": "packages/core/src/field.ts",
          "symbol": "FIELDS",
          "note": "The one query key that does not describe the view, and the only one that falls back instead of refusing a bad value. This list is the source of truth for the CSS blocks, the contrast rows and the catalog table."
        },
        {
          "file": "apps/web/src/field.ts",
          "symbol": "applyFieldToDocument",
          "note": "The DOM half. Called from main.tsx before React mounts, because applying a field from an effect paints the default desk for one frame first — a white flash on `?field=dark`."
        },
        {
          "file": "scripts/gate-params.mjs",
          "symbol": "parseBranches",
          "note": "Binds parseHash, serializeState and docs/query-params.md to each other. It reads the failure-mode column against the code: a branch that can `return null` rejects, one that cannot falls back. It also binds tokens.css to DEFAULT_FIELD, because the document is painted before field.css is parsed and a drift there would show on exactly one frame."
        }
      ],
      "extend": [
        "Add the key to `AppState`, then to both serializeState and parseHash in the same commit — a round-trip test is the cheapest way to keep them honest.",
        "Decide, in writing, whether a bad value refuses the whole link or falls back to a default. Reject is the rule; falling back needs the argument `field` makes, which is that the key does not describe the view.",
        "Add its row to docs/query-params.md, failure mode included — `gate:params` fails on a key with no row, and on a row that disagrees with the code.",
        "Old links must keep working. Treat the hash format as a published API, because it is: it is in people's notes and messages.",
        "Add an e2e that opens the URL cold, rather than one that clicks its way to the state — those test different things and only the first tests a link."
      ],
      "gates": ["pnpm test", "pnpm gate:params", "make e2e"],
      "seeAlso": ["the-hop", "offline-pwa"]
    },

    {
      "id": "wayfinding",
      "what": "Getting to a known place: the surah/ayah jumper, the page bar along the bottom, the keyboard map, and the coach marks that teach the gestures once.",
      "layer": "L2 web",
      "entry": [
        {
          "file": "packages/core/src/jump.ts",
          "symbol": "parseJump",
          "note": "Parses what someone actually types — Arabic or Western digits, Arabic names with or without alif-lam, '2:255', 'p19'. `normalizeArabic` is the reason it tolerates real input."
        },
        {
          "file": "apps/web/src/components/Jumper.tsx",
          "symbol": "Jumper",
          "note": "The combobox, opened from the one glyph in the header that is not a mode. The row it lives in has 17 px of slack on a 320 px phone — see chrome-fit before adding anything to it."
        },
        {
          "file": "apps/web/e2e/chrome-fit.spec.ts",
          "symbol": "WIDTHS",
          "note": "The header fits every phone from 320 to 430, and is the same height at all of them. The second half is the one that bites: the stage gets what the chrome leaves, and the golden baselines are shots of the stage."
        },
        {
          "file": "packages/core/src/pages.ts",
          "symbol": "nearestPage",
          "note": "The seam between the book's length and this build's inventory. The track spans the print (EditionMeta.pages — 604, and absent for editions nobody has counted); the landing is whatever is in public/assets. `nearestPage` answers with what it *did*, so the caller can say so."
        },
        {
          "file": "apps/web/src/components/PageSlider.tsx",
          "symbol": "PageSlider",
          "note": "The bottom bar: a 604-long track with a page turn on each edge, pinned RTL in both languages so page 1 sits on the right. Two traps live here — it commits on the native `change` event, not React's `onChange` (which is `input`, and would mount a 170 KB SVG per value crossed), and it takes the arrow keys itself, because a range's own step of 1-of-604 snaps straight back inside a three-page inventory."
        },
        {
          "file": "packages/core/src/keymap.ts",
          "symbol": "appKeyAction",
          "note": "One pure function from (key, context) to action, so the keyboard map is testable without a DOM and cannot disagree with itself across components."
        },
        {
          "file": "apps/web/src/components/CoachMarks.tsx",
          "symbol": "CoachMarks",
          "note": "Shown once, keyed in localStorage. Every e2e that is not about coach marks must suppress them in an init script, or it is testing the coach marks."
        }
      ],
      "extend": [
        "New input syntax: extend parseJump and its unit tests. Keep the UI dumb — it should render JumpTargets, not know how they were found.",
        "New shortcut: add it to appKeyAction with a KeyContext branch, and add the row to the help surface in the same commit.",
        "Anything added to the header row costs width the row does not have, and height the stage cannot spare. Run chrome-fit before and after, and if it needs a control, say which one it replaces.",
        "Vendoring pages: the page bar's *copy* needed no change when Loop 4b filled the print — the track was already the whole print, so it went from «٣ من ٦٠٤» to «٦٠٤ من ٦٠٤» on its own. `pagesVendored` stays: it is not a caveat that expires, it is the bar saying what is behind it, and the next edition may carry half a mus'haf. Its *picture* did need a change and did not get one for a loop: the inventory was drawn one span per vendored page, which was three marks at three pages and 604 overlapping ones at 604 — a solid rail saying nothing, reconciled on every value a dragged thumb passed over. It is drawn as `pageRuns` now, one node per contiguous stretch, so the cost follows the number of gaps rather than the length of the book. The lesson generalises past this bar: a picture whose node count is the size of the corpus is a picture that was only ever tested on a small corpus. The snap is the other thing to watch — it used to fire on nearly every drag and now fires almost never, so e2e/pagebar.spec.ts trims a page out of the manifest to keep testing the branch rather than testing that it never runs.",
        "The page bar is the bottom-most chrome, so it owns the home-indicator inset. If anything is ever put below it, the inset moves with it — two elements padding for it is a doubled gap, neither is a control under the indicator."
      ],
      "gates": ["pnpm test", "make e2e"],
      "seeAlso": ["a11y", "editions-and-skins"]
    },

    {
      "id": "editions-and-skins",
      "what": "Which mushaf print you are reading, and whether tajweed colouring is on.",
      "layer": "L1 core",
      "entry": [
        {
          "file": "packages/core/src/concordance.ts",
          "symbol": "EDITIONS",
          "note": "The registry. `EditionStatus` is what keeps an edition whose provenance is still PROVISIONAL out of the picker."
        },
        {
          "file": "packages/core/src/concordance.ts",
          "symbol": "class Concordance",
          "note": "Maps an ayah between editions. Needed because ayah numbering is not universal and a share link must survive an edition switch."
        },
        {
          "file": "packages/core/src/skins.ts",
          "symbol": "class Tajweed",
          "note": "Per-surah mark shards → per-ayah marks. `TAJWEED_RULES` is the rule vocabulary and the palette keys derive from it."
        },
        {
          "file": "apps/web/src/components/SkinToggle.tsx",
          "symbol": "SkinToggle",
          "note": "The switch. Skin swap must not re-render the stage — it changes classes on existing nodes."
        }
      ],
      "extend": [
        "A new edition is a data change first: assets, a SOURCES.md entry with its licence, and a concordance. Only then a registry entry.",
        "gate:license and gate:text-sources both read SOURCES.md — an edition whose provenance is not recorded cannot ship, by construction.",
        "A new tajweed rule: add to TAJWEED_RULES, emit from build-tajweed.mjs, add the palette entry. The class prefix is centralised so CSS and TS cannot drift."
      ],
      "gates": ["pnpm test", "pnpm gate:license", "pnpm gate:text-sources", "make ci"],
      "seeAlso": ["edge-data-etl", "provenance"]
    },

    {
      "id": "ui-language",
      "what": "Which language the chrome speaks — and the much longer list of things that never change with it.",
      "layer": "L2 web",
      "entry": [
        {
          "file": "apps/web/src/lang.ts",
          "symbol": "LOCALES",
          "note": "What a locale has to declare before it can render anything: its name in itself, the abbreviation of that name the desktop header's switch wears, its chrome direction, its surah-name table, its digits, and which spelling of a tajweed rule it puts first. Keyed by a `Lang` derived from the catalogs, so dropping in messages/ur.json fails to compile until Urdu has answered all six. None of them has a defensible default — a locale silently inheriting `ltr` is a mus'haf rendered backwards, and nothing shortens «العربية» to «ع» by rule, which is why `abbr` is declared here rather than truncated at the call site."
        },
        {
          "file": "apps/web/src/lang.ts",
          "symbol": "detectLang",
          "note": "The opening language: a stored choice, then the device, then Arabic. React-free and CSS-free on purpose — main.tsx calls it before mount so the document's lang/dir are right on the first paint, and e2e/lang.spec.ts imports LANG_STORAGE_KEY from here rather than retyping it."
        },
        {
          "file": "apps/web/src/messages/ar.json",
          "symbol": "distancePages",
          "note": "The reason this is a framework and not a table. Arabic has six CLDR plural categories and `few` is n % 100 = 3..10, not n <= 10 — which is what the hand-written ternary said, wrongly, for 41 of the 603 distances the mus'haf can produce. ICU MessageFormat states the six forms; Intl.PluralRules picks between them. Compiled at build time by scripts/messages-compile.mjs; the parser is a devDependency and ships zero bytes."
        },
        {
          "file": "apps/web/src/i18n.tsx",
          "symbol": "Strings",
          "note": "The shape every component reads, unchanged by the migration to compiled catalogs so that no call site moved. It is no longer where completeness is enforced — that is one layer down, in `const messages: Catalog` inside each messages/*.gen.ts, which is stricter because it checks argument names and not just arity. There is still no runtime lookup that can miss and no fallback chain."
        },
        {
          "file": "apps/web/src/i18n.tsx",
          "symbol": "buildStrings",
          "note": "The assembler: one function, run for every locale, wiring a compiled catalog to format.ts's formatters and to LOCALES. Written once is the point — the old file said everything twice, so «صفحتان» and \"2 pages\" were two independent chances to bind a rule differently. It also holds the numeral convention: {n} selects a plural form, {nText} is digits(n, lang) and is what prints, and a bare {page} is a raw number, i.e. Latin digits in every language."
        },
        {
          "file": "apps/web/src/i18n.tsx",
          "symbol": "LangProvider",
          "note": "Binds the chosen bundle, the formatters and `dir` into one context. Mounted in main.tsx, not App: the default context is Arabic with an inert setter, so every component test written before this feature renders bare and still reads Arabic."
        },
        {
          "file": "scripts/gate-i18n.mjs",
          "symbol": "gate:i18n",
          "note": "The four holes tsc cannot see: generated code left stale after a catalog edit, a key the reference locale has and a translation does not, a plural that writes four of Arabic's six categories, and a translation that drops a placeholder. Plural completeness is checked against Intl.PluralRules(locale) — the same CLDR the runtime will use, not a table in this repo. Registered in package.json, the Makefile and ci.yml; a gate in one of the three does not run."
        },
        {
          "file": "apps/web/src/format.ts",
          "symbol": "surahNames",
          "note": "Names and numerals are presentation, so they live here and not in core — `parseJump` takes the table as an argument, the same injection `enhancePolygons` uses for labelFor. Every function in this file takes an explicit `lang` and none defaults it."
        },
        {
          "file": "apps/web/e2e/lang.spec.ts",
          "symbol": "the mus'haf, the rail, the trail and the page bar stay right-to-left",
          "note": "The assertion that matters most, and the one unit tests cannot reach: with an English chrome, `main` and `footer` still compute `direction: rtl`. The stylesheet is 100% logical properties, which is what makes the flip a one-line change and also what makes an accidental inherited flip move the hop rail to the wrong thumb."
        }
      ],
      "extend": [
        "New user-facing copy is a key in every messages/*.json, then `pnpm i18n:build`, then a line in buildStrings. Never a literal in a component — the compiler is what stops an English build shipping an Arabic sentence, and a key missing from one locale does not compile.",
        "Anything that counts takes two arguments: {n} for the plural to select on and {nText} for the digits that print. The emitter rejects ICU's `#` and {n, number} on purpose — they route through Intl.NumberFormat, which is a second numeral authority and disagrees with format.ts's digits().",
        "Adding a language is a JSON catalog plus a LOCALES row; there is no union to widen and no import to add, because Lang and CATALOGS are derived from the files in messages/. The full checklist is docs/design/i18n.md §⑦.",
    "Both language switches — Colophon's and DesktopChrome's — map LOCALE_IDS over LOCALES, so a third locale grows a third option with no edit. Do not reintroduce anything shaped like `other`: a field meaning \"the language this one is not\" is well defined at two locales and silently wrong at three, where it renders one option short and puts the same name on two buttons. The tests are written over LOCALE_IDS for that reason and will name the language the switch left out.",
        "Ask whether the new text is chrome or scripture. Chrome translates and follows `dir`; anything quoting the mus'haf, a verse, a root, or a licence credit pins `lang=\"ar\" dir=\"rtl\"` and is translated by nobody.",
        "i18n.test.tsx walks LOCALE_IDS and calls every formatter, so a new key is covered the day it exists and a new locale the day its catalog lands.",
        "The e2e suite is pinned to `locale: \"ar\"` in playwright.config.ts because that is what every aria snapshot and golden image was recorded in. lang.spec.ts is the one file that overrides it, and a second such file should say why."
      ],
      "gates": ["pnpm test", "pnpm gate:i18n", "make e2e", "make ci"],
      "seeAlso": ["a11y", "wayfinding", "provenance"]
    },

    {
      "id": "desktop-spread",
      "what": "On a wide screen the app shows an open mus'haf — two leaves and a spine — and says plainly which half it does not have.",
      "layer": "L2 web",
      "entry": [
        {
          "file": "docs/design/desktop.md",
          "symbol": "Hifth is a navigation instrument for huffaz",
          "note": "Read this before touching anything below. It carries the arithmetic behind the breakpoint, the governing rule for what desktop is allowed to add, and §4's table of what changed when the print was vendored. docs/decisions/desktop-vs-mobile.md is the running table of individual decisions; append a row there rather than restating one here."
        },
        {
          "file": "packages/core/src/pages.ts",
          "symbol": "spreadOf",
          "note": "Which two pages face each other. The mus'haf reads right to left, so the *lower* number is the right-hand leaf, and the print pairs (1,2), (3,4)… — odd on the right, which is why Al-Fatiha faces Al-Baqarah and why 604 pages make 302 openings with no orphan. This is pagination, not presentation, which is why it sits in core beside nearestPage and why no component re-derives it; its doc comment records where the parity was observed and why the phase it used to implement was wrong."
        },
        {
          "file": "apps/web/src/useMediaQuery.ts",
          "symbol": "DESKTOP_QUERY",
          "note": "The one breakpoint, and the only copy of record. Two axes because a mus'haf leaf is portrait: leaf width comes from the height the chrome leaves, so a width-only query would hand a 1440x700 window less page than a 320px phone. Repeated as a literal in DesktopChrome.module.css only because CSS cannot read a custom property inside a media query; useMediaQuery.test.ts reads that stylesheet back so the two cannot drift."
        },
        {
          "file": "apps/web/src/components/PageSpread.tsx",
          "symbol": "PageSpread",
          "note": "The two leaves. Sides are declared exactly once — right leaf first in DOM order, inside main[dir=\"rtl\"] — so there is no row-reverse or `order` that could disagree with appKeyAction's ArrowLeft = +1. `enabled` gates the *mount*, not the styling: a display:none leaf still fetches its ~170 KB SVG and builds a Highlighter. Three boxes, and the two testids are how tests tell them apart: .spread (page-spread) is the desk and runs the window's width, .book (page-book) is the two leaves and nothing else — it carries the gutter, clips the parked band, and is what the fold portals into — and .leaf is one page, sized block-size:100% + aspect-ratio 345/550 rather than flex: 1 1 0."
        },
        {
          "file": "apps/web/src/components/PageStage.module.css",
          "symbol": "--stage-page-cap",
          "note": "One of three knobs (with --stage-field and --stage-pad) that are the whole coupling between the spread's stylesheet and the stage's. CSS modules hash their class names, so a custom property is the only way one can size the other; every fallback is the phone's own behaviour, so PageStage needed no change at all. In a spread all three are neutralised — cap 100%, field none, pad 0 — because the leaf is already exactly a page and the desk already painted the field."
        },
        {
          "file": "apps/web/src/components/DesktopChrome.tsx",
          "symbol": "DesktopChrome",
          "note": "The controls a phone had no room for (the language switch, two levels deep in the colophon sheet because chrome-fit holds the header inside 320px with 17px of slack) and no use for (the keyboard map), plus the two that exist because a desktop has a second leaf to close: the page-mode radiogroup and the zoom stepper. Hidden by CSS rather than unmounted, unlike the spread — these are spans and buttons, not a second mus'haf. The chrome decides nothing: it renders `pageMode`/`zoom` and calls back. The stepper is disabled while the book is open and says why on the control itself, which is the ordering that replaced a derivation — see ZOOM_STEPS."
        },
        {
          "file": "apps/web/src/components/PageStage.tsx",
          "symbol": "ZOOM_STEPS",
          "note": "The nine rungs the stepper climbs, exported because the chrome needs to know where the ends are — a disabled button at the floor is the ladder being stated rather than discovered by clicking. A ladder and not a multiplier because a button is pressed a countable number of times and should land on a nameable level; the spacing keeps the old wheel ratio's feel, and `rung(z, step)` takes the nearest rung STRICTLY past z, because zoom also arrives from a hop at DEFAULT_HOP_ZOOM 1.55, which is not on the ladder. Every zoom on this stage goes through zoomAbout, and setZoom anchors at the LAYER centre — never the stage, never the host. Do not write a second copy of that arithmetic; pass an origin. docs/decisions/wheel-and-zoom.md."
        },
        {
          "file": "packages/core/src/packs.ts",
          "symbol": "juzPageIndex",
          "note": "Thirty entries, the first page of each juz, or null for a juz this build does not carry. Built on juzOfPage rather than on a second membership rule — the repo forbids a second copy of that. Memoised once at the App level because its caller is a wheel: shift+wheel is an array index, not a 604-page scan. A juz boundary page belongs to two juz at once and this deliberately disagrees with juzOfPage about page 22 — juzOfPage calls it juz 1's, this calls it juz 2's START, and both are true of the same sheet."
        },
        {
          "file": "apps/web/e2e/desktop.spec.ts",
          "symbol": "ctrl+wheel does nothing at all — it is somebody else's pinch",
          "note": "The assertion that a binding is absent, which is the only kind that rots silently. On macOS a trackpad pinch arrives as ctrl+wheel with the modifier synthesised by the OS, so anything bound here fires on a pinch — bound to juz it teleports ~20 pages, left unbound it hands the window to browser page zoom, which changes what a CSS pixel is and bounces DESKTOP_QUERY. So it is preventDefaulted and dropped, and this row is what stops someone reclaiming it. The jump lives on shift+wheel, which reads deltaY || deltaX because several browsers deliver a shifted wheel horizontally."
        },
        {
          "file": "apps/web/e2e/desktop.spec.ts",
          "symbol": "puts the lower page number on the right",
          "note": "The assertion the unit tests cannot make. They check DOM order; DOM order only becomes *sides* once an RTL flow has run, so a flex-direction: row-reverse added by someone 'fixing' the layout passes all nine component tests and puts the mus'haf on backwards. Verified by inducing exactly that."
        }
      ],
      "extend": [
        "Before adding anything to the desktop layout, name the mobile constraint that put it out of reach. If you cannot, it is a new feature and belongs in the PLAN, not in a media query — that is the governing rule in docs/design/desktop.md §1.",
        "A new desktop decision is a row appended to docs/decisions/desktop-vs-mobile.md: what mobile does, what desktop does, and why they differ. The design doc links to that table rather than duplicating it.",
        "Anything mounted only at desktop width must be gated in JS via useMediaQuery, not hidden in CSS, whenever mounting it costs a fetch or a Highlighter. Anything that is only markup should be CSS-hidden so it leaves the accessibility tree and the header's intrinsic width.",
        "A new desktop surface needs a row in e2e/contrast.spec.ts SURFACES, and that row must call page.setViewportSize — both projects running that file are phones, so without the resize the row looks like coverage and measures nothing.",
        "Loop 4b vendored the print, so `renderFacing` is no longer fixture-only — every spread is two real leaves, and the honest hole is now reached by trimming a page out of the manifest (apps/web/e2e/inventory.ts). Nothing about the geometry changed; see docs/design/desktop.md §4 for the table of what did.",
        "The wheel navigates and buttons magnify. Do not derive page mode from zoom, or zoom from anything: that was built, and one state with two owners leaked three ways (the facing leaf zoomed alone; a breakpoint crossing kept the mode and lost the view; the range below fit read as 'at fit'). pageMode and zoom are explicit state in App, written in one place, and the chrome only asks. docs/decisions/wheel-and-zoom.md.",
        "Anything gated on 'is the book open' must ask `desktop && pageMode === \"two\"`, never pageMode alone. pageMode is real state at every width so a resize cannot lose the reader's choice, but it defaults to \"two\" where no spread renders — reading it alone silently withheld the hop's magnification on every phone, and the golden images are what caught it."
      ],
      "gates": ["pnpm test", "make e2e", "pnpm gate:budget", "make ci"],
      "seeAlso": ["wayfinding", "ui-language", "a11y", "assets-and-pages"]
    },

    {
      "id": "offline-pwa",
      "what": "Works with the phone in aeroplane mode, a week after you last opened it.",
      "layer": "L2 web",
      "entry": [
        {
          "file": "apps/web/src/pwa.ts",
          "symbol": "initPwa",
          "note": "Registration and update flow. Service workers need a secure context — that is why offline specs cannot pass against a plain-http LAN or container URL, and it is the harness, not the app."
        },
        {
          "file": "apps/web/src/pwa.ts",
          "symbol": "repairShellCache",
          "note": "Eviction takes the precache and leaves the registration, so the worker never installs again and offline dies silently while every online signal still says healthy. The repair re-registers the same sw.js under a query-suffixed URL, because only a script the registration has never seen produces an install — measured, not assumed: reloads, update(), and unregister()+register() all leave the precache empty or resurrect the old worker. It waits for that install to finish, not for index.html to reappear: the shell is the first entry written and the app's scripts are the last."
        },
        {
          "file": "apps/web/src/storage.ts",
          "symbol": "requestPersistentStorage",
          "note": "Asks the browser not to evict us. It can say no, and the honest UI for that is a notice, not silence."
        },
        {
          "file": "apps/web/src/components/OfflineNotice.tsx",
          "symbol": "OfflineNotice",
          "note": "What the user is told about their storage — and the only place the install prompt is offered. Must not stack with the coach strip; must not move into the header, which has no width for it. Loop 6b gave it a third thing to say: a pinned juz that a sweep took, which is the one storage message that arrives with a repair rather than a warning."
        },
        {
          "file": "packages/core/src/packs.ts",
          "symbol": "planPack",
          "note": "Loop 6b: which files a pinned juz is made of. Read off the manifest's pages, never off JUZ_STARTS — the table says where a juz begins, the manifest says which paper exists, and a plan built from the first would promise pages the build has no SVG for. It counts those absent ayahs instead of hiding them, which is why the offer can say so before the reader spends the bytes."
        },
        {
          "file": "apps/web/src/packs.ts",
          "symbol": "PACK_CACHE",
          "note": "A pack lives in its own Cache Storage bucket with no expiration plugin, and assets.ts reads it before the network. Warming the service worker's hifth-pages instead would have been simpler and is wrong: that cache is a 32-entry LRU sized for a browsing trail, so pinning a juz and then reading twelve pages evicts a third of what was pinned, and nobody finds out until the plane. Bytes in Cache Storage, the claim in IndexedDB — the split is what makes a sweep detectable rather than merely silent."
        },
        {
          "file": "apps/web/src/packs.ts",
          "symbol": "PIN_HEADER",
          "note": "The one thing a pin tells the service worker: this fetch is not a read for display. Both runtimeCaching routes in vite.config.ts decline anything carrying it, so a pinned file is written once instead of also landing in hifth-pages and evicting the trail it was meant to preserve (backlog ⑮). The literal is deliberately duplicated into vite.config.ts and must stay in step: workbox generateSW stringifies a route matcher, so an imported constant reaches the worker as undefined and the route silently declines nothing. Static provenance is the whole contract — the worker never learns PACK_CACHE's name, the pack's URL list, or that a register exists, so planPack can change shape without sw.js moving. See docs/decisions/pin-marker.md."
        },
        {
          "file": "apps/web/src/components/PackShelf.tsx",
          "symbol": "PackShelf",
          "note": "The pin control and the shelf of what is kept, inside the revision map at juz scope — the map is already the picture of the thirty, and the header has no room for a sixth button. The states are named in words, in a list, not painted as a dot on a cell: the cells already carry warmth and absence, and the meaning that would be misread is exactly the one that matters, a swept pack looking pinned."
        },
        {
          "file": "apps/web/e2e/offline.spec.ts",
          "symbol": "dropOutsidePack",
          "note": "Pinning writes every file twice — pinPack fetches through the page, so the service worker's CacheFirst route caches the same bytes — which means an offline test can pass with the pack deleted. Deleting the outside copy is not a workaround: it is the state a pinned juz is genuinely in once ordinary reading pushes it out of that LRU, and the only state in which the assertion means what it says."
        }
      ],
      "extend": [
        "Precache list and strategies live in the vite-plugin-pwa config in apps/web/vite.config.ts, not in a hand-written sw.js.",
        "Adding to the precache costs install-time bytes on a phone. Check the entry count and size the build prints.",
        "Test offline behaviour in an e2e that actually goes offline, over a secure context. A unit test cannot see a service worker.",
        "Eviction is a separate case from offline and has its own describe in apps/web/e2e/offline.spec.ts: CDP Storage.clearDataForOrigin with storageTypes 'cache_storage' takes the caches and leaves the worker registered, which is the state a real ITP sweep leaves behind.",
        "Anything the reader chose to keep does not go in a runtime cache. It goes in the pack cache, and something has to be able to notice it is gone — a store with no register against it can only ever report success.",
        "A new runtimeCaching route needs the !request.headers.has(\"X-Hifth-Pin\") clause too, or pinning starts filing duplicates in it. From a controlled page there is no fetch the worker cannot see — cache.add is routed the same way, measured — so a pin is recognised and declined rather than hidden. What the worker may know about a pack stops at that one bit; anything about the pack's shape belongs in packs.ts.",
        "Anything that can fail to load a page must say so — see PageStage's error banner. A silent failure leaves the previous page on the stage while the chrome has already announced the new number."
      ],
      "gates": ["make e2e", "make lighthouse"],
      "seeAlso": ["share-links", "assets-and-pages"]
    },

    {
      "id": "assets-and-pages",
      "what": "The mushaf SVG pages themselves, and the polygon metadata that makes them clickable.",
      "layer": "L3 etl",
      "entry": [
        {
          "file": "packages/etl/scripts/extract-pages.mjs",
          "symbol": "polygonsOf",
          "note": "Source SVG → the page asset plus its polygon manifest. Deterministic by requirement: CI re-runs it and fails on any byte of difference."
        },
        {
          "file": "apps/web/src/assets.ts",
          "symbol": "loadPage",
          "note": "Fetch and cache a page in the browser. The one place that knows the asset URL shape."
        },
        {
          "file": "packages/core/src/mounted-set.ts",
          "symbol": "retainPages",
          "note": "The ceiling on how many page SVGs stay in the DOM (backlog ③), and the reason vendoring 604 pages did not turn a long reading session into a leak: `MOUNTED_PAGE_CAP` mounted nodes, evicted least-recently-used, with the pages a turn is animating between pinned so the leaf being turned cannot be collected mid-flight. A spread mounts two leaves at once, so `spreadBudget` splits the same cap rather than doubling it — App.tsx passes each stage its share."
        },
        {
          "file": "packages/etl/scripts/vendor-pages.mjs",
          "symbol": "LOOP0_PAGES",
          "note": "Loop 4b: the whole print, fetched from the pinned upstream, optimised the way Loop 0 optimised its three, and written unpadded (`8.svg`, not `008.svg`). It reproduces pages 7, 9 and 19 byte for byte before it will vendor anything else — a run that cannot rebuild what is already committed has changed the pipeline, not extended the corpus. Three transforms stand between upstream and what ships, all declared in the header and all asserted by exact count: svgo at the pinned version and config, `ID_REPAIRS` (two polygons whose `id` holds path geometry), and `POLYGON_REPAIRS` (PLAN 14 — 21 tap targets that did not cover their own ayah). The last runs *before* svgo, because svgo rewrites path data at `floatPrecision 1` and a repair written against optimised output would be matching our own rounding rather than upstream's bytes."
        },
        {
          "file": "packages/core/src/manifest.ts",
          "symbol": "expandManifest",
          "note": "The wire form the app fetches. 604 pages of per-ayah polygons is megabytes; the compact form is one 6236-long array indexed by absolute ayah, 1.3 KB gzipped, so the manifest stayed a single eager request instead of becoming a shard. A page an edition lacks is written as `0` and skipped on expand — that zero is what every honest-hole path downstream is reading."
        },
        {
          "file": "scripts/gate-pages.mjs",
          "symbol": "PIN_FILE",
          "note": "Cross-checks the vendored pages against the QUL V2 pin: the count, the filename shape, and the anchor ayahs on sampled pages. `audit-corpus` asks whether the manifest covers the print; this asks whether the paper under it is the print we pinned. Since PLAN 14 it also asks the one question every identity check misses — is every glyph of scripture inside a box a finger can reach — and it asks it by measurement, not by signature: map the ink through the page matrix and test each glyph's centre against the polygons. The two signatures that preceded it pattern-matched shapes already known and missed seven pages, four of them because the rule excused any page whose first polygon is an `X:1`. What replaced them is `ORPHAN` (a glyph outside every polygon and every furniture band) and `BAND` (a furniture band taller than two line-heights, so it has swallowed scripture), both demanding zero — possible only because the 12-unit tolerance was measured (benign glyphs stray 8.8 units, defects a median 36) rather than chosen. Furniture is inferred, never listed: it is a gap in the polygon tiling that a surah's first ayah picks up directly below. There is deliberately no allow-list — this gate knows nothing of `vendor-pages.mjs`'s repair table, so a regressed repair and a newly-broken upstream fail alike."
        },
        {
          "file": "packages/etl/scripts/audit-corpus.mjs",
          "symbol": "MADANI_TOTAL_PAGES",
          "note": "Does the corpus actually cover what we claim? Coverage assumed rather than measured is how a missing page reaches a memoriser."
        },
        {
          "file": "scripts/gate-assets.mjs",
          "symbol": "MAX_PAGE_GZ",
          "note": "What the corpus weighs, and the ceilings. pages/ is gated per page plus a 604-page projection rather than by a total, because a total ceiling on the one kind that grows would have to be raised by the very change it exists to watch."
        },
        {
          "file": "packages/etl/scripts/probe-ligature-print.mjs",
          "symbol": "BANDS",
          "note": "Asks of a corpus we have not adopted the one question adoption turns on: which print does it paginate? Reads the candidate's own `data-surah`/`data-aya` and compares each page's ayah span against `ayah-pages.json` — no fonts, no rendering, no judgment. The page set is every V1/V2 divergence band plus controls outside them, because contested pages alone cannot separate \"this corpus is V2\" from \"our table is wrong in exactly the contested places\". Named `probe-` and not `gate-`: it reaches the public internet and answers a question, and its answer lives in `ligature-svg.probe.json` beside the fetched pages' hashes."
        },
        {
          "file": "packages/etl/scripts/probe-word-registration.mjs",
          "symbol": "fit",
          "note": "The second question about the same unadopted corpus, and the one word-level selection was blocked on: do their word boxes land on our paper? Fits a 4-parameter axis-aligned affine per page from the one landmark both corpora carry independently — the ayah-end ornament — and then checks every word centre falls inside the ayah polygon its own attributes name. The answer (`word-registration.probe.json`) is that scale is 1.3333 both axes and the offsets are the two constants already sitting in our own SVGs' transform matrices, one per page parity; residual is sub-pixel, so the per-page fit is a check rather than a necessity. Reuses `ligature-svg.probe.json`'s pin — one pin, not two — so a corpus that moves invalidates both probes at once."
        }
      ],
      "extend": [
        "Never hand-edit anything under apps/web/public/assets — it is generated, and the ETL determinism check in CI will reject it.",
        "A corpus considered and not adopted still leaves evidence: probe it against a pinned commit, hash what you fetched, and commit the verdict (`ligature-svg.probe.json`). A decision remembered instead of recorded gets re-litigated by the next loop that wants word geometry.",
        "Two coordinate spaces live in one page SVG: the ink sits under `matrix(1.3333 0 0 -1.3333 -115|-55 640)` and is y-flipped, the ayah polygons sit outside it in plain top-down viewBox space. Anything mapping external geometry onto a polygon works in the second space and must not apply the matrix — `probe-word-registration.mjs`'s header records how that was established.",
        "Change the producing script, run `make etl`, and commit the script and the regenerated output together.",
        "gate:notext enforces that pages carry no <text> nodes: the glyphs are outlines, so rendering never depends on a font being installed.",
        "A new kind of asset needs a ceiling in gate-assets.mjs CEILINGS before it can land — the gate fails on a directory it does not recognise, on purpose, because silently skipping the unfamiliar is the defect it closes."
      ],
      "gates": ["pnpm gate:notext", "pnpm gate:assets", "pnpm audit:corpus", "make ci"],
      "seeAlso": ["edge-data-etl", "golden-images", "word-geometry"]
    },

    {
      "id": "word-geometry",
      "what": "A rectangle per word on the mushaf page — measured off a second print of the same mus'haf and moved onto ours.",
      "layer": "L3 etl",
      "entry": [
        {
          "file": "packages/etl/scripts/lib/mushaf-frame.mjs",
          "symbol": "fitFrames",
          "note": "The one copy of the arithmetic that puts two prints of the same page into one coordinate system. Both corpora mark the end of every ayah with an ornament, so the ornaments are a correspondence neither was built to provide: `readTheirs`/`readOurs` pull them out, `readingOrder` puts both lists in the same order (line bands right-to-left, which is the only ordering an RTL page can be paired in), and `fit` solves `ours = s·theirs + t` per axis by least squares. Everything else here is the plumbing that has to be exact for that to mean anything — `pathBBox` with real cubic extrema rather than control-point hulls, `ringsOf`/`pointInRings` for testing a point against a multi-subpath polygon. It is a library and not a script because three callers need identical answers; two copies of a least-squares fit that disagree in the sixth decimal are two different mus'hafs."
        },
        {
          "file": "packages/etl/scripts/lib/candidate-pages.mjs",
          "symbol": "candidatePage",
          "note": "One fetch of the upstream corpus, cached on disk under `data/pages/.cache/words`, hashed on the way in and checked against `ligature-svg.probe.json`'s pin on the way out. 378 MB is read to produce 2.2 MB, and it is read by a probe and a builder that must see the same bytes — so `{ offline: true }` is a real mode, not a convenience: it refuses to reach the network and fails if the cache is cold, which is how a determinism re-run proves it re-derived rather than re-downloaded."
        },
        {
          "file": "packages/etl/scripts/build-words.mjs",
          "symbol": "MAX_RESIDUAL",
          "note": "The builder: 604 shards of word boxes on our viewBox, plus `word-boxes.pin.json` (a SHA-256 per upstream page and per shard). Per page it fits the frame, transfers every `<g id=\"md-word-…\">`'s bbox through it, and asserts the fit's residual is under a ceiling — 2.0 units on the standard frame, 3.0 on the two override pages, both measured against an observed max of 2.722 rather than picked. Pause marks are separated from lexical words (`isPauseMark`) because they are the only boxes that legitimately sit outside their ayah's polygon, hanging below the line. A page whose ornaments cannot be paired borrows its parity group's median transform rather than guessing, and says which pages borrowed."
        },
        {
          "file": "scripts/gate-words.mjs",
          "symbol": "MARK_DROP",
          "note": "Re-measures every committed box against the polygon it claims — offline, and with its own path parser rather than the ETL's, because a gate that imports the code it is checking only proves the code agrees with itself. Three checks: LEXICAL (a word's centre is inside its own ayah), MARK (a pause mark's bottom edge meets its polygon within `MARK_DROP`, 4 units — measured: 4,453 of 4,486 need nothing and the worst needs 2.9), PAIRING (shard keys and polygon keys match in both directions, 604 pages, zero either way). This is the gate that found the one defect `gate:pages` structurally cannot see: on p577 the ink of 75:5's first word *was* covered by a tappable box — the wrong ayah's. No orphan, so no orphan check could fire. Two prints of the same page can say that; one cannot."
        }
      ],
      "extend": [
        "The word index here is the *print's*, not the Quranic Arabic Corpus's — the print counts pause marks as words, and the two disagree on 4,499 of 6,236 ayahs. That alignment now exists and is a separate feature, `word-indexing`; do not re-derive it here, and do not renumber these boxes to match QAC. The number on a box is the number the geometry arrived with, and the map is what relates it to anything else.",
        "No upstream bytes ship and none should start. The pages stay the quranpedia print; what this area contributes is numbers. If a change would vendor their ink or their text, it is a new source and needs a SOURCES.md entry of its own, not an extension of this one.",
        "Anything that transfers external geometry onto a page works in plain top-down viewBox space, outside the `matrix(1.3333 0 0 -1.3333 …)` the ink sits under. See assets-and-pages' note on the two coordinate spaces.",
        "A tolerance added here is measured before it is written, and the measurement goes in the comment beside it — `MAX_RESIDUAL` and `MARK_DROP` both name the distribution they clear. A number with no distribution behind it is an allow-list wearing a constant's clothes.",
        "`gate:words` deliberately duplicates parsing and point-in-polygon. Do not refactor it to import `lib/mushaf-frame.mjs`; the duplication is the check."
      ],
      "gates": ["pnpm gate:words", "pnpm gate:assets", "pnpm probe:word-registration", "make ci"],
      "seeAlso": ["assets-and-pages", "select-an-ayah", "word-selection", "word-indexing"]
    },

    {
      "id": "word-indexing",
      "what": "One word, two numbers — the print's, which every box carries, and the Quranic Arabic Corpus's, which the roots data speaks — and the measured map between them.",
      "layer": "L3 etl",
      "entry": [
        {
          "file": "packages/etl/scripts/lib/segmentation.mjs",
          "symbol": "alignBlocks",
          "note": "The one copy of the alignment, in two halves: the aligner (needs the upstream corpus) and the reader (needs only this repo). `alignBlocks` is the method — both sides fold to one Buckwalter skeleton, and a block boundary can only fall where the two cumulative lengths agree, so the alignment either partitions both sequences or does not exist. There is no score and no threshold to tune, which is deliberate: a fuzzy aligner would have produced a number for all 6,236 ayahs, four of them wrong, and nothing would have said which four. `EXCEPTIONS` names those four with a reason each. `Alignment` is the reader — `mapOf`, `qacWordOf`, `printWordsOf`, `qacCount` — and it applies the delta rather than looking a table up."
        },
        {
          "file": "packages/etl/scripts/build-alignment.mjs",
          "symbol": "rebuild",
          "note": "The driver and the report. `pnpm align --rebuild` reads all 604 cached upstream pages, aligns per ayah, and FAILS if the unaligned set is not exactly the four in `EXCEPTIONS` — the exceptions are the measurement, not a filter applied to it, and silently widening them is how an allow-list starts. Everything else is a lookup off the committed pin: `pnpm align 2:4`, `--print N`, `--qac N`. Two modes because the rebuild needs a 378 MB cache and the question a developer actually asks — which QAC word is this box? — should not."
        },
        {
          "file": "packages/etl/data/pages/word-alignment.pin.json",
          "symbol": "$method",
          "note": "The map, as a DELTA over the shipped word shards rather than a table: `j` lists the print indices that continue the QAC word their predecessor started, `s` the one print word (15:7) that covers two, and everything else is recovered from the shards. Storing it outright would restate the shards 86,965 times and let the two disagree; a delta cannot disagree with its base, it can only fail to apply. 4,496 of 6,236 ayahs carry one. `$method` and `measured` carry the numbers a reader would otherwise have to rerun for: 6,232/6,236 aligned, 86,965 print words to 77,429 QAC words, three block shapes, and the hamza fold's own measurement (276 failures without it, 4 with)."
        },
        {
          "file": "scripts/gate-align.mjs",
          "symbol": "expectedMeasured",
          "note": "Applies the map offline and checks it against a third witness. The pin cannot be re-derived in CI — the upstream corpus is not vendored — which is exactly the situation the mutashabihat off-by-one lived in for four loops. So this gate reads the committed shards (which print indices exist, which are marks), applies the pin's delta, and compares the resulting QAC word count against the vendored morphology, which neither the pin nor the shards control. It also holds `EXCEPTIONS` and the pin's copy against each other in both directions, and re-derives `measured` rather than trusting it."
        },
        {
          "file": "packages/etl/scripts/probe-tajweed-words.mjs",
          "symbol": "fold",
          "note": "The third index nobody planned for: the tajweed annotations are CODEPOINT offsets into a Tanzil Uthmani text this repo does not hold, so neither word index reaches them. This probe reconstructs that text from the print's own per-word `data-hafs` and measures how many print words each annotation touches. Seven corrections make the fold agree, each earned by a failed run. Three are STRUCTURAL and the corpus states each — prepend the basmala to ayah 1 of every surah but 1 and 9, glue the `data-waw-alatf` split waw to its successor, and DROP the pause-mark words the print numbers and Tanzil does not carry (63.40% → 97.03%). Four are ORTHOGRAPHIC and live in `RESPELL`, found by bracketing each failing ayah between its last correct annotation and its first wrong one: a small high mark on a tatweel carrier, «أٓ» spelled with one codepoint more there, the small high madda, the hamza below (97.03% → 99.81%). Correction 4's narrowness is measured, not chosen — stripping every U+0640 scores 94.48%, worse than nothing — which is also the standing guard against overfitting. The fold is not trusted on its own output: `hamzat_wasl` must start on ٱ and `lam_shamsiyyah` on ل, 15,985 annotations that can be checked with no reference to any word boundary. `NAMED` carries the eleven survivors and the run prints an ⚠ for any residual ayah it does not name, so a moved pin announces itself. Named `probe-` and never a gate for the same reason as its two neighbours, plus one of its own: it reads the gitignored 378 MB cache, so on a clean checkout it has nothing to read."
        },
        {
          "file": "packages/etl/data/tajweed/tajweed-words.probe.json",
          "symbol": "$verdict",
          "note": "What ⑤ was waiting for. The oracle lands 99.81%, 83.31% of the 60,057 annotations fall inside one print word, 16.69% touch two ADJACENT words — the cross-word phonology, idghaam and ikhfa and iqlab, which are two-word by nature and paintable on both boxes — exactly 1 touches more than two and 0 run past the end. The residual is ELEVEN ayahs (0.18%) and it is orthographic rather than structural: all 30 misses sit within ±2 codepoints and every ayah drifts by a single constant amount. It is not a rate — `residual.named` names each one, and 12:39, 12:41 and 15:7 are exceptions `segmentation.mjs` ALREADY names for the print↔QAC alignment, reached here from a third text neither of those involves. That is the strongest evidence the class is orthographic. So ⑤'s premise is falsified: a build change with an eleven-entry exception list, not a third text."
        }
      ],
      "extend": [
        "The map stays L3 and the app still cannot read it — on purpose, and that is now a shipped answer rather than an open one. word-D chose placement C (`docs/design/word-indexing.md` §11 ①): the ETL applies the map and BAKES ITS ANSWER into `assets/adj/**` and `assets/roots/**` as print indices, so nothing in L1 or L2 imports `Alignment` and no client can get the delta application wrong. The cost of restating an answer instead of shipping a question was measured at 8.9× the estimate (+81.6 KB gz) and paid deliberately. A THIRD consumer needs its own baking pass in ETL — the tajweed shards have not had one — not a browser-side reader.",
        "The tajweed shards are the third consumer, and their bake is now PRICED but not built. `probe-tajweed-words.mjs` measured the fold ⑤ asked for: 99.81% oracle, 83.31% of annotations inside one print word, an eleven-ayah orthographic residual with every entry named. What that buys is permission to write the bake as a build change; what it does not buy is the beta label, which waits on a hafiz. Re-run the probe before writing the bake — the numbers above are of one pin, and the pin can move; the run prints ⚠ for any residual ayah `NAMED` does not cover, which is the signal that it did.",
        "The alignment is DERIVED FROM the print's text and does not carry it. `build-words.mjs` still drops `data-hafs` and `gate:notext` still enforces it. Anything that would need the word's text to work is a new vendoring with its own licence question, not an extension of this.",
        "Do not widen `fold()` to make the four exceptions go away. The fold's value is that it is measured; four named exceptions checked in two places is a smaller surface than a fold nobody can argue with. A fifth exception means measure again, not add a name.",
        "Neither index may be renumbered. Both are upstream identifiers — the print's is load-bearing for every shard and its pin, QAC's for `assets/roots/**`, `gate:edges` and the tajweed ETL. The map exists so that neither has to move.",
        "`gate:align` implements the delta application in its own ten lines rather than calling `Alignment.mapOf`. Do not refactor it to import the reader; what is being checked is the pin, and the reader is part of what could be wrong with it.",
        "The map is per-edition by construction (`EDITION`). A second print needs its own rebuild, its own exceptions and its own measurement; nothing here generalises for free."
      ],
      "gates": ["pnpm gate:align", "pnpm gate:words", "pnpm gate:edges", "make ci"],
      "seeAlso": ["word-geometry", "root-lens", "word-selection", "provenance"]
    },

    {
      "id": "word-selection",
      "what": "Hold inside an ayah you have already selected, and the grain gets finer: the run under your finger is words, and dragging extends it.",
      "layer": "L1 core",
      "entry": [
        {
          "file": "packages/core/src/words.ts",
          "symbol": "class WordIndex",
          "note": "The reader for one page's word shard, and the whole reason word selection can be a gesture rather than a lookup. `wordAt` is nearest-word *within the named ayah*, skipping pause marks — not strict containment — because a finger dragging along a line spends most of its time in the gaps between words, and a containment test would make the band flicker off between every pair. It returns null only for an ayah with no boxes on this page. `bandsFor` collapses a from→to run into one Rect per line of print, which is what makes a three-line run three swipes and not thirty."
        },
        {
          "file": "packages/core/src/gestures.ts",
          "symbol": "heldIntent",
          "note": "The fifth verdict, and the one separated from an existing one by a single bit of context: a hold is a `word` run if it began inside the current selection and a `marquee` if it did not. That is the whole risk of the feature — a bug in reading that bit does not produce a broken word run, it produces the Loop 5 marquee vanishing — so `word.spec.ts` drives both from the same hold on the same page."
        },
        {
          "file": "packages/core/src/highlighter.ts",
          "symbol": "consumePress",
          "note": "How the ladder tells the tap detector, in advance, that a stroke is spoken for. `onPolygonPointerUp` scores a tap by travel, and for a hold that measurement is exactly backwards: a long-press that releases where it began has travelled nothing, so it read as a second tap on the ayah already selected — and `App.handleSelect` toggles the selection *off* on a second tap. A reader refining their selection lost it. Called for any latched non-tap intent rather than for `word` alone, because a small marquee had the same shape of bug and a `word`-only guard would be a second place obliged to agree with `nextIntent` about which verdicts are terminal."
        },
        {
          "file": "apps/web/src/components/PageStage.tsx",
          "symbol": "applyWords",
          "note": "One frame of a word stroke: put the point on a word, ink anchor→cursor into the `word` group, and on release say which words those were. The band is drawn with the `\"sel\"` style over an ayah wash that is still lit — `mix-blend-mode: multiply` is what makes the overlap read as a darker emphasis rather than a second slab, and it is the reason this feature has a golden row at all. `clearWords` is its counterweight: a range and a word run answer different questions about the same page, so only one may be on it."
        },
        {
          "file": "apps/web/src/assets.ts",
          "symbol": "loadWordShard",
          "note": "The only loader keyed by page rather than by surah, because that is how the geometry is stored: a word box is a position on a sheet of paper, so its shard is the sheet. Fetched the moment a hold latches, ~3.6 KB, deliberately not precached — so the first drop into words on a cold page is a round trip, and quiet on a miss: the reader loses the finer grain, not the ayah they had."
        }
      ],
      "extend": [
        "`onSelectWords` fires with a `<ayah>#w3-7` key on release — the ayah key with the run appended — and `App.handleSelectWords` is what hears it: `Adjacency.hopsForWords` narrows the edge list to the ones the run is about, and the count is announced. Two rules a second consumer has to keep. The key's indices are the PRINT's — the print↔QAC alignment was applied in ETL and baked into the shards, never here (see `word-indexing`). And the roots must be handed in from `Roots.rootsForWords`, because `Adjacency` holds no root shards: without them a shared-root edge lands in `unplaced` rather than silently passing, which is the honest reading and the reason the refinement returns three outcomes instead of two.",
        "A word run announces its OUTCOME and never its words. «٧ مواضع مشابهة» is the answer the reader dragged for; naming the run would put scripture into a UI string, which is the one thing this grain exists not to do (`docs/design/word-indexing.md` §10 and §11 ②). The overlay stays decorative — `word.spec.ts` asserts the stage's accessibility tree is identical before and after a run — so anything a word run has to say goes through the app's one live region and not through a node on the stage.",
        "The keyboard descent is claimed in the CAPTURE phase on `window`, above the highlighter's own bubble-phase listener on the SVG: `Enter` on an already-selected ayah descends, `←`/`→` carry the run, `Shift` extends it, `Escape` climbs one rung. That ordering is the whole mechanism — it needs no L1 change, and it is why the arrows do not fall through to the page-turner. `word.spec.ts` proves the page did not turn by reading the slider's `aria-valuetext`, because a page that turned and a page that never loaded look identical in the SVG.",
        "The e2e nudge is not decoration. `filterTaps: true` gives @use-gesture's drag a 3 px threshold, so a hold that moves 1 px produces no frame and the ladder is never consulted — a spec that presses and waits will watch the feature do nothing. Both `word.spec.ts` and the golden row carry `NUDGE_PX` and say why.",
        "A golden that photographs a *gesture* must aim with the paint, not with a bounding box: the golden row takes the widest selection band because it is a whole line of print with both ends known, and aiming by box put the press 12 px from the window edge with the run growing off-screen."
      ],
      "gates": ["pnpm test", "make e2e", "make golden", "make golden-linux"],
      "seeAlso": ["select-an-ayah", "word-geometry", "golden-images"]
    },

    {
      "id": "edge-data-etl",
      "what": "Turning the mutashabihat dataset into the per-surah shards the app hops through.",
      "layer": "L3 etl",
      "entry": [
        {
          "file": "packages/etl/scripts/build-adjacency.mjs",
          "symbol": "addEdge",
          "note": "Dataset + curated edges → 114 shards. Reverse edges are generated here, so an edge stated once is navigable from both ends."
        },
        {
          "file": "packages/core/src/quran-meta.ts",
          "symbol": "toAbsoluteAyah",
          "note": "The 6236-ayah numbering the datasets speak, and its inverse. Every off-by-one bug in this project has lived within one function of this pair."
        },
        {
          "file": "packages/etl/scripts/build-adjacency.mjs",
          "symbol": "datasetAbs",
          "note": "The +1 that bridges a 0-based dataset to a 1-based core. It shipped missing, and 47.8% of the hop corpus pointed at an ayah sharing no words with its source — a mutashabihat instrument being quietly, entirely wrong. Nothing caught it: the curated Loop-2 edges are written as \"S:A\" strings and skip this conversion, so every gate and every e2e stayed green over the only edges anyone had looked at. The comment above it carries the measurement; read it before touching numbering here."
        },
        {
          "file": "scripts/gate-edges.mjs",
          "symbol": "ZERO_OVERLAP_LIMIT",
          "note": "The gate that would have caught the off-by-one on day one. Every dataset-derived edge is scored by the longest contiguous run of words its two ends share, and CI fails past 10% of them sharing none. Random ayah pairs score 69% there, the broken corpus scored 48%, the corrected one 3.9% — the threshold sits in a gap, not on a tuning knob."
        },
        {
          "file": "packages/etl/scripts/morphology.mjs",
          "symbol": "longestSharedRun",
          "note": "The one reader of the vendored morphology, and the one definition of 'how similar are these two ayahs'. The gate enforces a floor on this number and the sampler prints it beside each pair a human audits — computed a second time, they would eventually disagree, and the human would be the one assumed wrong."
        },
        {
          "file": "packages/etl/scripts/sample-edges.mjs",
          "symbol": "stratumOf",
          "note": "The spot-audit draw. Stratified by type × provenance × page distance, because 97% of the pairs are one class and a flat draw of twenty never shows the classes gate:edges cannot reach. The unit is the unordered pair, not the directed edge — the corpus generates both directions from one fact, so asking a reader about both spends the scarcest input this project has on a question already answered."
        }
      ],
      "extend": [
        "Vendoring a new dataset: add the SOURCES.md entry with its licence in the same commit as the data. gate:license fails otherwise, deliberately.",
        "Every script prints a summary line — counts in, counts out, largest shard. Read it; a silent 10% drop in edges looks exactly like success.",
        "Human verdicts on specific edges are permanent. They go in the verified-edges register, and gate:verified-edges enforces them from then on.",
        "Data here can be structurally perfect and semantically noise; gate:edges is the only check that reads meaning. A new edge type whose promise is shared phrasing belongs in its SCORED_TYPES, or it ships unmeasured.",
        "A new edge type also becomes a new stratum in the spot-audit draw automatically, and `make validate` will show it as a class nobody has looked at until someone does."
      ],
      "gates": ["pnpm gate:verified-edges", "pnpm gate:edges", "pnpm gate:license", "make ci"],
      "seeAlso": ["the-hop", "validation", "provenance"]
    },

    {
      "id": "revision-record",
      "what": "What the reader has opened, by day and by page, hizb or juz — the log, and the picture drawn from it. Never leaves the device.",
      "layer": "L1 core",
      "entry": [
        {
          "file": "packages/core/src/revision.ts",
          "symbol": "RevisionEvent",
          "note": "Start here, and read the doc comment before the fields. It enumerates what must NOT become an event — a toggle-off, a hop arrival, a share-link arrival — because recording those evenly maps app usage rather than revision, and the two diverge exactly where the record was meant to be useful. The `tz` field rides on the event rather than being passed at read time: a single offset applied at display time re-files every look recorded on the other side of a DST change or a flight."
        },
        {
          "file": "packages/core/src/revision.ts",
          "symbol": "scopesOf",
          "note": "Scopes are `page`, `hizb` and `juz`. A page yields exactly one id; a hizb or a juz can straddle, and then the event credits both. Unparseable input yields `[]`, so a corrupt record is a gap in the picture rather than a wrong colour. `hizb` only exists here because HIZB_STARTS is vendored — the tempting derivation, half a juz, is wrong, and a scope whose boundaries are guessed is a heatmap that lies about which ayahs it is colouring."
        },
        {
          "file": "apps/web/src/revision-store.ts",
          "symbol": "readRecord",
          "note": "The only impure half: the one place that reads a clock and touches storage, and therefore the one place that can lie. Note that opening the store stamps `since` — iOS deletes script-writable storage after seven days of no interaction, so the record has to know how young it is or an emptied log reads as «you have revised nothing»."
        },
        {
          "file": "apps/web/src/revision-store.ts",
          "symbol": "pruneOnce",
          "note": "Retention, run once per calendar day from both the write path and the read path. A boolean got this wrong: a session left open across midnight never pruned again, and a read-only session — open the app, look, close — never pruned at all and handed the picture rows the store had promised to have forgotten."
        },
        {
          "file": "apps/web/src/App.tsx",
          "symbol": "recordLook",
          "note": "The two honest signals, and the whole wiring: a tap inside the `!toggledOff` branch, and a marquee release as one event carrying both ends. `handleHop` and the hash router set the selection past `handleSelect` on purpose, which is what makes the three exclusions hold by construction rather than by discipline."
        },
        {
          "file": "scripts/gate-revision-privacy.mjs",
          "symbol": "ALLOWED_IMPORTERS",
          "note": "A log of when someone was reading Qur'an is not a preference. This is a closed allow-list of modules permitted to import the record, plus a grep for escape hatches inside the record's own files. Deliberately not `import/no-restricted-paths`, which expresses the inverse rule and would leave every new URL-building file unguarded by default."
        },
        {
          "file": "apps/web/src/components/RevisionMap.tsx",
          "symbol": "holdings",
          "note": "The picture's half of the honesty problem: when 3 of 604 pages were vendored, a division the reader could not reach had to not be drawn as one they neglected — absent is no fill and a dashed hairline, cold is the sunk paper, and the two differ in kind rather than in opacity. `holdings` answers that by pushing every vendored ayah through the same `scopesOf` the record uses, so «absent» and «warm» cannot drift about which hizb a page is in. It was `coverage` and returned a set; it returns a map now, because the same sweep that says «is there paper here» can say «and the lowest page of it we hold» for free, and that number is where a press on the cell lands (`docs/design/revision-record.md` ②). Lowest, not first-encountered: nothing in `PageMeta` promises the manifest is ascending. Read off the manifest and never off `HIZB_STARTS`, which knows which *ayah* opens a hizb but not which page of *this* build prints it — a landing the build cannot show is unrepresentable rather than a case to remember. The sheet opens from the page chip, not a sixth header button: `e2e/chrome-fit` holds the row inside 320px with seventeen pixels to spare."
        },
        {
          "file": "apps/web/e2e/revision.spec.ts",
          "symbol": "a tap on an ayah is still on the map after a reload",
          "note": "The only test that can prove a tap became durable: the unit suite hands the component a record, and a store that wrote to memory would pass all of it. Note `recorded()` — the write is fired and forgotten from the tap handler, so a reload can outrun it; and it must be `expect.poll` over `evaluate`, because `waitForFunction`'s poller does not await a promise and would succeed on the first tick regardless of what the database holds."
        },
        {
          "file": "apps/web/src/App.test.tsx",
          "symbol": "credits the source of a hop, never the ayah the app moved them to",
          "note": "The three exclusions asserted end to end through the real UI. `revision.test.ts` and `revision-store.test.ts` can prove what a look is worth; only this file can prove App.tsx does not call `recordLook` from the three places it must not."
        },
        {
          "file": "packages/core/src/quran-meta.ts",
          "symbol": "HIZB_STARTS",
          "note": "The boundaries a per-hizb heatmap will be labelled with — sixty vendored [surah, ayah] pairs, not thirty juz halved. Tanzil publishes no hizb element at all: the division is 240 quarters and a hizb is four of them. The shortcut agrees for 4 of 30 and misses by up to 39 ayahs, which is the whole reason this is a table."
        },
        {
          "file": "scripts/gate-quran-meta.mjs",
          "symbol": "onMidpoint",
          "note": "What keeps the tables honest: AYAH_COUNTS, JUZ_STARTS and HIZB_STARTS are re-derived from packages/etl/data/meta/quran-data.xml on every CI run and diffed. The last check is the interesting one — if more than 4 of 30 even hizbs sit on their juz's midpoint, someone regenerated the table by halving, and the gate says so by name instead of letting sixty plausible numbers through."
        }
      ],
      "extend": [
        "Anything that reads the record is a new importer: add it to ALLOWED_IMPORTERS in the gate with a one-line reason. If it cannot state a reason it cannot see the record.",
        "Adding a scope means adding real boundary data first. `hizb` was the standing example and is now the worked one: the arithmetic derivation is wrong, so the sixty starts are vendored from the Tanzil metadata and `gate:quran-meta` re-derives them on every run. A scope whose boundaries are guessed is a heatmap that lies about which ayahs it is colouring, and no other test here would notice.",
        "New callers of `recordLook` must justify themselves against the `RevisionEvent` doc comment. The default answer is no: the record is only worth drawing because it is not a log of everything that happened.",
        "The warmth bands (2/7/30 days in RevisionMap.tsx) are a developer's guess and are named as one in docs/design/revision-record.md. Changing them wants a hafiz, not an argument. What is not negotiable is the axis: recency, which is why `lastSeen` exists apart from `rollUp`.",
        "The map is a way in, not only a picture: a held cell is a `<button>` that opens its division, an absent cell stays a plain `<li>` with nothing to press, and the whole 604-cell grid is one tab stop (a roving `tabindex`, arrows stepping over the paper we do not have). New cells inherit that shape — a control that refuses is worse than an outline that never offered, and 604 tab stops inside a Tab-trapped dialog is a map nobody can leave. Note `focusables()` here is no longer Colophon's verbatim copy: it excludes `tabindex=\"-1\"`, or the trap's `last` element lands in the middle of the grid.",
        "Navigation from the map deliberately does not write to the record: `recordLook` is called only from the two honest signals in App.tsx, so pressing a cell cannot warm the cell you just pressed. A page-turn added anywhere else must keep that property, or the picture starts drawing the reader looking at it.",
        "Loop 4b vendored the other 601 pages and the map itself needed nothing — `holdings` reads the manifest — but the e2e did, exactly as predicted: it had pinned hizb 3 as the first division with no paper and the inventory as «المتوفّر ٢ من ٦٠ حزب», and both failed loudly. It is «٦٠ من ٦٠» now, and «absent is not cold» is held by a second row that trims pages 22–31 out of the manifest. Keep that row: with a complete edition the absent treatment has no way to appear on its own, and it is what the map owes the next edition that does not carry the whole print."
      ],
      "gates": ["pnpm test", "pnpm gate:revision-privacy", "pnpm gate:quran-meta", "make ci"],
      "seeAlso": ["select-an-ayah", "offline-pwa", "validation"]
    },

    {
      "id": "a11y",
      "what": "Usable by someone who cannot see the page: labels on the polygons, a live region for what changed, and Arabic digits everywhere.",
      "layer": "L1 core",
      "entry": [
        {
          "file": "packages/core/src/highlighter.ts",
          "symbol": "enhancePolygons",
          "note": "Where an invisible polygon becomes a focusable, labelled control. If a new drawn thing should be reachable by screen reader or keyboard, it happens here."
        },
        {
          "file": "apps/web/src/components/LiveAnnouncer.tsx",
          "symbol": "LiveAnnouncer",
          "note": "One aria-live region for the whole app. Several would race and interrupt each other."
        },
        {
          "file": "apps/web/src/format.ts",
          "symbol": "export function digits",
          "note": "Every number a user hears or reads, in the digits their UI language reads them in — `toArabicDigits` beneath it is the Arabic half. The bug this prevents is a badge showing ٣ while the label says 'three'; invisible until an aria snapshot puts the two side by side. The `lang` parameter is required on purpose: a default would let a new call site render Arabic-Indic digits inside an English sentence and still compile."
        },
        {
          "file": "apps/web/e2e/share-a11y.spec.ts",
          "symbol": "aria snapshots",
          "note": "The accessible tree, committed as text under e2e/__aria__/. Snapshots are text, not geometry, so they are keyed by project and not by platform — a Linux runner and a Mac must produce the same tree or one of them is wrong."
        }
      ],
      "extend": [
        "Any new control: a label in Arabic, a 44px target, and a visible focus ring. The contrast spec checks the third and will fail you.",
        "Numbers in user-facing strings go through `digits(n, lang)` — including inside aria-labels, which is the half everyone forgets. Never `String(n)`, and never `toArabicDigits` directly: one is wrong in Arabic and the other is wrong in English.",
        "Update the aria snapshots deliberately (`--update-snapshots`) and read the diff. An accepted snapshot is the only place a broken tree can hide."
      ],
      "gates": ["make e2e", "make lighthouse"],
      "seeAlso": ["wayfinding", "golden-images"]
    },

    {
      "id": "golden-images",
      "what": "The visual gate: highlight geometry the DOM cannot describe, pinned as images.",
      "layer": "gate",
      "entry": [
        {
          "file": "apps/web/playwright.config.ts",
          "symbol": "snapshotPathTemplate",
          "note": "Golden images are keyed by {platform} because they are rasterized geometry. The aria snapshots beside them are keyed by {projectName} because they are text — mixing the two axes is a bug in both directions."
        },
        {
          "file": "Makefile",
          "symbol": "GOLDEN_IMAGE",
          "note": "The pinned container. The generator and the checker must be the same rasterizer or the gate is a coin flip; gate:golden-env enforces that the Makefile, the CI job, and the installed Playwright all agree."
        },
        {
          "file": "scripts/gate-golden-size.mjs",
          "symbol": "BUDGET",
          "note": "Why the baselines are committed to git rather than LFS, and the number at which that answer should be revisited."
        }
      ],
      "extend": [
        "Adding a row means adding baselines for BOTH platforms: `make golden-update` for darwin, `make golden-linux UPDATE=1` for linux.",
        "Open the changed PNGs before committing. An accepted baseline is the gate agreeing with you, which is worth nothing unless you looked.",
        "Never bump Playwright without bumping GOLDEN_IMAGE and re-rendering — the failure mode is ten red diffs that look exactly like a UI regression."
      ],
      "gates": ["make golden", "make golden-linux", "pnpm gate:golden-env", "pnpm gate:golden-size"],
      "seeAlso": ["select-an-ayah", "word-selection", "a11y"]
    },

    {
      "id": "validation",
      "what": "The checks a machine cannot do — a phone in a hand, a mushaf on a table — tracked so they cannot quietly not happen.",
      "layer": "gate",
      "entry": [
        {
          "file": "docs/validation/ledger.json",
          "symbol": "runbook",
          "note": "The only place a manual runbook is written. `make validate CHECK=<id>` prints it, `make guide` renders it for a phone, the /validate skill drives it."
        },
        {
          "file": "scripts/gate-validation.mjs",
          "symbol": "needsRunbook",
          "note": "Does NOT fail on outstanding checks — a phone that has not been held is a fact, not a broken build. It fails when the ledger lies."
        },
        {
          "file": "scripts/record-validation.mjs",
          "symbol": "check.verifiedOn",
          "note": "Writes the verdict back and then prints what that verdict now obliges — the standing rule is that a manual result must tighten something automated."
        },
        {
          "file": "scripts/validate-auto.mjs",
          "symbol": "records",
          "note": "The only writer of docs/validation/evidence/. Runs each check's declared `evidence.run` and writes the real exit code; everything else only reads. Exits 0 even when a producer goes red — a reporting run, not a gate."
        },
        {
          "file": "scripts/validation-ledger.mjs",
          "symbol": "outcomeOf",
          "note": "Exit 3 is \"could not tell\", and it is not a pass — so a producer that could not reach its subject strikes no step off a human's runbook."
        },
        {
          "file": "scripts/validation-ledger.mjs",
          "symbol": "evidencePath",
          "note": "Derived from the check id, never stored beside it: a path written down in two places is a path that can disagree with itself."
        },
        {
          "file": "scripts/probe-reference.mjs",
          "symbol": "V1_V2_DIVERGENCE",
          "note": "The one part of edge-spot-audit a machine can reach: a second opinion on the print, fetched rather than held. api.quran.com publishes a V1/1405H page table and ours is V2/1421H, so the two must disagree on exactly 36 pages — which turns a useless source into a fingerprint, and makes agreement where they should differ as much a finding as the reverse. `probe-` and not `gate-` because it reaches the network, same reasoning as check-source-offer.mjs. Keys only, no `fields`, so no Quran text crosses the wire."
        },
        {
          "file": ".claude/skills/mushaf-reference/SKILL.md",
          "symbol": "name: mushaf-reference",
          "note": "The prose half of the probe: which published references are reachable (measured), what each can and cannot settle, and the trap — six of archive.org's twelve KFGQPC scans are a different qira'a or script, so comparing against one produces confident, wrong verdicts. The symbol is the frontmatter name and not a section heading because scripts/code-pointers.mjs reads a leading `#` as a comment, so no Markdown heading can ever be a live pointer; the name is the better anchor anyway, since renaming the skill is the change that would actually strand this row."
        }
      ],
      "extend": [
        "A new manual check is a ledger entry with a full runbook. A pending user-owned check with no steps fails the gate, because a check nobody can follow will not be run.",
        "Edit the ledger and re-run `make guide` in the same commit — a staleness hash fails the build otherwise.",
        "When a check comes back, record it and then do what `tunes` printed. That step is what turns a verdict into a permanent test instead of a note.",
        "An `evidence` block must name a `residue` — say in words what the command cannot do. The gate rejects one that does not, because an automated run naming no remainder is one claiming to have done a human's job.",
        "`evidence.covers` points at runbook step ids, never positions, and `evidence.run` is a script rather than a make target — make flattens every failed recipe to exit 2, which would let a network timeout read as a verdict.",
        "Before asking a human for a mushaf, run `make probe-reference ALL=1` and see what is left. It cannot judge whether two ayahs resemble each other — nothing on a network can — but it settles every arithmetic claim underneath that question, and it must never become a gate: a check that reaches the internet teaches everyone to skip it the first time a host is down."
      ],
      "gates": ["pnpm gate:validation", "make ci"],
      "seeAlso": ["edge-data-etl", "provenance"]
    },

    {
      "id": "provenance",
      "what": "Every glyph on screen traces to a named source under a named licence, and the offer to hand that source over resolves.",
      "layer": "gate",
      "entry": [
        {
          "file": "SOURCES.md",
          "symbol": "hafs-kfqc",
          "note": "The register of everything vendored. Not documentation — two gates parse it, so an entry is a load-bearing part of the build."
        },
        {
          "file": "scripts/gate-license.mjs",
          "symbol": "problems",
          "note": "Fails if something is vendored without a recorded licence. The obligation is legal, so the check is mechanical."
        },
        {
          "file": "apps/web/src/components/Colophon.tsx",
          "symbol": "CREDITS",
          "note": "What the user is actually told. Not authored here — each row is declared in its SOURCES.md ```colophon fence and this renders it, so the record and the screen cannot drift."
        },
        {
          "file": "scripts/gate-license-copy.mjs",
          "symbol": "NON_COMMERCIAL",
          "note": "Enforces that binding, and refuses a non-commercial claim nobody recorded — the exact defect that once told every reader the KFGQPC artwork was more restricted than it is."
        },
        {
          "file": "apps/web/src/provenance.ts",
          "symbol": "SOURCE_REPO",
          "note": "Where the offer points. It has to actually resolve for a stranger — that is a real obligation, not a nice-to-have."
        },
        {
          "file": "scripts/check-source-offer.mjs",
          "symbol": "probe",
          "note": "The offer, followed the way a stranger would follow it: anonymously, no gh and no token, because signed in as ourselves a private repo looks public. Named `check-` and not `gate-` because it reaches the public internet and is deliberately not in `pnpm gates` — it runs on demand (`make source-offer`) and from the public-deploy workflow."
        }
      ],
      "extend": [
        "Vendor nothing without its SOURCES.md entry in the same commit.",
        "To change what the reader is told about a licence, edit that source's ```colophon fence in SOURCES.md. The component follows; gate:license-copy fails if it does not.",
        "A source the app should not credit still has to say so — `not-credited: <reason>` in the fence. Silence is indistinguishable from an oversight, which is why it is not allowed.",
        "A GPL-derived asset makes this repo the corresponding source. That constrains unrelated decisions, including whether binaries can live behind a metered service (see gate:golden-size).",
        "Before publishing, run `make source-offer URL=<deployed>`. It is red today on purpose: the repository is private, so the offer 404s for everyone who is not us (task #53)."
      ],
      "gates": [
        "pnpm gate:license",
        "pnpm gate:license-copy",
        "pnpm gate:text-sources",
        "make ci",
        "make source-offer"
      ],
      "seeAlso": ["editions-and-skins", "assets-and-pages", "golden-images"]
    },

    {
      "id": "ci-and-gates",
      "what": "What judges a change, and the rule that every gate is reproducible locally.",
      "layer": "gate",
      "entry": [
        {
          "file": "Makefile",
          "symbol": "ci:",
          "note": "An exact mirror of the build-test-gate job, in CI order. If you add a step to one, add it to the other in the same commit — that promise is the whole value of the target."
        },
        {
          "file": ".github/workflows/ci.yml",
          "symbol": "build-test-gate",
          "note": "Four jobs: secrets-scan, build-test-gate, e2e (containerised), lighthouse. Only the second is what `make ci` mirrors; the other three have their own make targets."
        },
        {
          "file": "scripts/gate-gates.mjs",
          "symbol": "owned",
          "note": "The `extend` rule below, enforced. Every gate:* script in package.json must be invoked by `pnpm gates`, by the Makefile ci target, and by a workflow job — and every gate named in those three must exist. Written after the rule was broken twice in prose-only form: gate:pages was merged wired to nothing at all, and gate:edges ran in `make ci` but in no job, which is the worse direction because the author sees a pass the branch protection never saw. Three places to remember is two too many; memory is not a mechanism. No exemption list, deliberately — a gate that genuinely cannot run in CI is a conversation, not an array entry."
        },
        {
          "file": "scripts/gate-ci-artifacts.mjs",
          "symbol": "PRODUCERS",
          "note": "Every artifact CI uploads is one something actually produces. Written after an upload step preserved nothing for several loops while looking green."
        },
        {
          "file": ".lighthouserc.json",
          "symbol": "maxNumericValue",
          "note": "Where Loop 4b's `< 2.5 s TTI on mid-Android` is actually evaluated. The four `categories:*` assertions cannot do it: Lighthouse 12 gives the TTI audit weight 0 in the `hidden` group, so a perfect 100 on performance promises nothing about it. The emulated device is written out in `collect.settings` rather than inherited from Lighthouse's default preset — inherited, an upgrade that retunes the preset would silently change which phone the criterion is about. Median of five runs, because the metric steps rather than scatters: it moves in whole `rttMs` units as the LCP resource lands one simulated round trip later or earlier."
        },
        {
          "file": "scripts/gate-budget.mjs",
          "symbol": "TOLERANCE_GZ",
          "note": "The 150 KB budget notices at the cliff; scripts/budget-baseline.json is how it notices on the slope. Every run prints a per-chunk delta and fails on a move of more than a kilobyte in either direction — a shrink too, because unrecorded headroom is headroom the next change spends unseen. The baseline is committed rather than built from main because that puts the number in the diff a reviewer is already reading instead of a CI log nobody opens, and because a merge-base build cannot run offline. Accept a move with `make budget-update`, and read the diff — an accepted baseline is the gate agreeing with you."
        },
        {
          "file": "scripts/gate-map.mjs",
          "symbol": "features",
          "note": "This map, checked. Every symbol listed in docs/map.json must still exist, so the orientation doc cannot rot into confident fiction."
        },
        {
          "file": "scripts/gate-use-cases.mjs",
          "symbol": "locateTest",
          "note": "The promises, checked. docs/use-cases.json names the test behind each thing this app claims to do; this asserts those tests still exist and are still tests. Rename a test and the commit doing it is told which promise it was standing behind."
        }
      ],
      "extend": [
        "A new gate is a script in scripts/, a `gate:*` entry in package.json, a link in the `gates` composite, a line in `make ci`, and a step in build-test-gate. All five, or it does not run — and gate:gates now says so out loud instead of leaving it to this sentence, which was written first and then broken twice.",
        "Verify a new gate by breaking the thing it protects and watching it fail, then reverting. A gate that has never failed has never been tested.",
        "Gates fail with the fix in the message. 'X is wrong' costs someone an afternoon; 'X is wrong, run Y' costs them a minute.",
        "A gate that guards a number with headroom needs a committed baseline as well as a ceiling, or it only speaks on the change that runs out. gate:budget is the shape: measure, commit the measurement, fail on drift, and let the baseline's diff be the report — the same trade the golden images make."
      ],
      "gates": ["make ci", "make gates", "make lighthouse"],
      "seeAlso": ["validation", "golden-images"]
    }
  ]
}
