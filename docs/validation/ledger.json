{
  "$comment": [
    "The manual-validation ledger: every check a machine cannot run, how to run",
    "it, and where its result went.",
    "",
    "Automated gates are self-recording — CI is either green or it is not. The",
    "checks that need a phone, a mushaf, or a pair of ears have no such memory:",
    "they get done once, written into a decision record in prose, and then nobody",
    "can answer 'is that still true, and on what device, and when?' without",
    "reading four documents. This file is that answer, in one place a gate can",
    "read (scripts/gate-validation.mjs).",
    "",
    "The point is not bookkeeping. It is that a manual result should end up",
    "tightening something automated — a threshold, a fixture, a new gate — so the",
    "expensive check does not have to be repeated to stay true. That is what the",
    "`tunes` field records: what this verdict is allowed to change. An entry whose",
    "`tunes` is empty is a check we are paying for and not banking.",
    "",
    "status:  pending | done | superseded",
    "blocks:  roadmap items that cannot proceed until this is `done`",
    "tunes:   the automated artifact this result feeds (threshold, fixture, gate)",
    "staleAfterDays: for repeating checks — how long a `done` result stays valid",
    "                before the gate asks for it again. Omit for one-shot checks.",
    "",
    "runbook: the check, written so someone holding a phone and no memory of the",
    "         conversation can run it. This is the ONLY place a runbook is",
    "         written; three renderers read it and nothing else restates it:",
    "           make validate CHECK=<id>   → the terminal",
    "           make guide                 → docs/validation/guide.html, on the phone",
    "           the `validate` skill       → drives a session end to end",
    "         Shape:",
    "           needs   — what you must have before starting (a device, a print, a deploy)",
    "           setup   — [{run, expect}] commands run here, on the laptop",
    "           steps   — [{do, expect, why, shot?}]",
    "                       do     — the action",
    "                       expect — what should happen if it is working. A step with",
    "                                no expectation cannot be failed, so it cannot be",
    "                                passed either.",
    "                       why    — what this step buys. An expectation you cannot",
    "                                justify is one a tired reader will wave through.",
    "                       shot   — id of a screenshot in docs/validation/shots/,",
    "                                captured from the real app by `make shots`, never",
    "                                hand-cropped. Only where a picture answers a",
    "                                question the prose cannot: what the screen looks",
    "                                like before you have ever seen it. The gate fails",
    "                                on a `shot` with no file behind it.",
    "           reading — how to turn what you saw into a verdict; one line per outcome",
    "           record  — the exact command that banks the result",
    "         A `pending` check owned by `user` with no runbook.steps fails the",
    "         gate: a check nobody can follow is a check that will not be run, and",
    "         it should not be able to sit here looking tracked."
  ],
  "checks": [
    {
      "id": "perf-verdict-on-device",
      "title": "On-device pan/zoom + highlight-toggle verdict",
      "why": "The emulated baseline (~8.3 ms/frame, flat under CPU throttle) structurally cannot see the two real risks: initial raster of a ~170 KB inline SVG on a low-end phone, and re-raster on zoom past the layer's backing store. The rendering-scale decision for 604 pages rests on this and nothing else.",
      "how": "make phone-perf → open the printed LAN URL on a real mid/low-tier Android and an older iPhone → tap ابدأ and follow the bar for 15s (pan, pinch, tap ayahs) → long-press the JSON it prints and paste it into this entry's `result`. No cable and no DevTools: the phone measures itself with a rAF sampler (apps/web/src/perf/probe.ts), because the old recipe — pair over USB, enable Web Inspector, find the timeline — is why this check sat open for six loops. Run it twice where you can, once as a browser tab and once from the Home Screen install; the JSON stamps which. The DevTools route still works if you want a flame chart: `make phone` serves the same build without the probe.",
      "decides": "inline-SVG everywhere | content-visibility virtualization | raster-glyph fallback",
      "owner": "user",
      "status": "pending",
      "blocks": [
        "loop-4b",
        "loop-6b",
        "loop-7"
      ],
      "runbook": {
        "needs": [
          "A real phone on the same Wi-Fi as this laptop — a mid/low-tier Android and an older iPhone if you have both; either one alone still answers most of it.",
          "No cable, no DevTools, no pairing. The page measures itself.",
          "About 3 minutes per device."
        ],
        "setup": [
          {
            "run": "make phone-perf",
            "expect": "Builds a throwaway probe bundle, prints `On your phone (same Wi-Fi): http://<lan-ip>:4173`, then holds the terminal open serving it. Leave it running."
          }
        ],
        "steps": [
          {
            "do": "Open that URL on the phone.",
            "expect": "The mushaf page as normal, plus a dark slab pinned across the top reading «قياس الأداء على هذا الجهاز — ١٥ ثانية» with an ابدأ button. No slab means you are on an ordinary build — something else is already serving port 4173; stop it and re-run make phone-perf.",
            "why": "Confirms you are measuring the build that has the probe in it. An ordinary build looks completely normal, which is exactly how you would not notice — and every number below would then be a measurement of the wrong thing.",
            "shot": "probe-idle"
          },
          {
            "do": "Tap ابدأ.",
            "expect": "«استعد / get ready» for one second before each segment, then the segment's instruction and a countdown. The slab ignores taps while recording, so you cannot accidentally drive it instead of the page.",
            "why": "The one-second «استعد» beat is there so the tail of one gesture does not land in the head of the next segment. Rush it and the three numbers blur into each other, which is the one way this check can fail without looking like it failed.",
            "shot": "probe-running"
          },
          {
            "do": "Segment 1 — «مرّر الصفحة بإصبع واحد»: pan the page with one finger, continuously, for the whole 5-second countdown.",
            "expect": "The page moves under your finger and the counter runs 5→1. Keep moving: a still finger produces no frames and the segment will report «too few frames — not driven».",
            "why": "Pan is the cheap case: no re-raster, just compositing a layer that already exists. If pan is over budget then nothing further matters — the page is too heavy as DOM and the verdict is raster-fallback."
          },
          {
            "do": "Segment 2 — «قرّب وباعد بإصبعين»: pinch in and out, going far enough that glyph edges visibly re-sharpen.",
            "expect": "Zoom follows the pinch. The re-sharpen moment is the one being measured — it is the layer being re-rastered.",
            "why": "This is the whole check. Zooming past the layer's backing store makes the browser re-rasterize ~170 KB of glyph paths, and that cost is structurally invisible to the emulated desktop baseline. The rendering decision for all 604 pages turns on this one number."
          },
          {
            "do": "Segment 3 — «اضغط آيات مختلفة»: tap a different ayah roughly once a second.",
            "expect": "Amber marker swipes land on each ayah you tap — one along the middle of every line it occupies, with the scripture still readable through them. Different ayahs, not the same one twice — you are measuring overlay churn.",
            "why": "Separates rendering cost from interaction cost. If only this segment is bad the problem is overlay churn in the highlighter — a real bug, but not one that changes which rendering strategy Loop 4b builds."
          },
          {
            "do": "Read the results table, then long-press the JSON box → Select All → Copy.",
            "expect": "«النتيجة — الصقها في ledger.json», a row per segment with mean / fps / p95 / worst / jank (over-budget cells in salmon), and a read-only JSON blob stamped with the device, dpr, cores, standalone flag, build commit, and FCP/LCP. There is no copy button on purpose: a LAN preview is plain http, so the Clipboard API does not exist here.",
            "why": "The table gives you the verdict; the JSON is what makes it re-checkable a year from now. It stamps device, dpr, cores, standalone flag and build commit, so a later reader can tell whether the number still describes the phone in their hand.",
            "shot": "probe-results"
          },
          {
            "do": "If you can, add the site to the Home Screen and run the whole thing again from there.",
            "expect": "`\"standalone\": true` in the second JSON. Standalone gets its own compositor path on iOS, so the two runs can legitimately disagree — that difference is a result, not noise.",
            "why": "iOS gives standalone its own compositor path. If the two runs disagree, the install prompt is doing load-bearing work rather than convenience work — and that changes what Loop 6b has to guarantee."
          }
        ],
        "reading": [
          "p95 ≤ 16.7 ms and jank < 10% on all three segments → verdict **inline-svg**: inline SVG everywhere holds, and Loop 4b is plain vendoring + streaming.",
          "Pan fine but pinch p95 much worse → re-raster past the layer's backing store → verdict **content-visibility**: virtualize off-screen pages; do not rasterize glyphs.",
          "Pan itself over budget, or worst > 100 ms on the first pinch → the page is too heavy as DOM → verdict **raster-fallback**: rasterize the glyph layer, keep the polygon hit layer as DOM (the highlighter API does not change).",
          "Only the tap segment is bad → that is INP, not rendering: overlay churn in packages/core/src/highlighter.ts. It does not change the rendering verdict; file it separately.",
          "«too few frames — not driven» on any segment → that segment never got a real gesture. Re-run it; a number from an undriven segment is not a result.",
          "The two devices disagree → the low-end one decides. The budget exists for the phone that struggles."
        ],
        "record": "make record CHECK=perf-verdict-on-device RESULT='inline-svg — Pixel 6a p95 14/23/9 ms (pan/pinch/tap), jank 6/18/2%; iPhone SE similar' — then paste the full JSON into docs/decisions/loop-4b.md"
      },
      "tunes": [
        "apps/web/perf/pan-zoom-trace.mjs — the frame budget asserted in CI becomes the measured number, not the emulated one",
        "docs/PLAN.md §Loop 4b — which of the three rendering strategies gets built"
      ],
      "record": "docs/decisions/loop-1.md → a new docs/decisions/loop-4b.md"
    },
    {
      "id": "screen-reader-walkthrough",
      "title": "VoiceOver / TalkBack gesture walkthrough",
      "why": "axe and the keyboard hop tour cover the machine-checkable floor and are green, but neither can tell you whether a screen-reader user can actually select an ayah and hop. Per-polygon labels are a design bet that has never been heard out loud.",
      "how": "On the same phone as perf-verdict-on-device: enable VoiceOver (iOS) / TalkBack (Android), then run the core tour by gesture alone. The accessibility tree — which controls exist, in what order, under what names — is pinned in apps/web/e2e/__aria__/ and re-checked on every run, so this check is now only the three things a runner cannot do: judge the phrasing by ear, try to escape a sheet by gesture, and do the whole tour without looking.",
      "owner": "user",
      "status": "pending",
      "blocks": [
        "loop-7"
      ],
      "runbook": {
        "needs": [
          "The same phone, with VoiceOver (iOS: Settings → Accessibility → VoiceOver, or triple-click the side button) or TalkBack (Android: Settings → Accessibility).",
          "About 8 quiet minutes. You are listening, not looking — the last step asks you to stop looking entirely.",
          "The ordinary build, not the probe build.",
          "Nothing to memorise. This used to be eight steps; five of them were checking that a control existed under a given name, and those are now aria snapshots in apps/web/e2e/__aria__/{iphone,android}/ that fail the build when a label changes. Reading those five files first is optional and takes a minute — they are exactly what the app should announce."
        ],
        "setup": [
          {
            "run": "make phone",
            "expect": "Prints `Open on your phone (same Wi-Fi): http://<lan-ip>:4173` and serves the normal app — no perf slab at the top."
          }
        ],
        "steps": [
          {
            "do": "Turn the screen reader on, open the URL, swipe into the mushaf, and listen to three or four ayah labels in a row. Judge the phrasing out loud. This is the bet under test.",
            "expect": "«الآية البقرة · ٢:٤٨» — that the label exists and reads exactly this is already asserted by e2e/__aria__/*/page-group.aria.yml. What you are judging is whether it sounds like something you would say, and whether your voice speaks ٢:٤٨ correctly rather than as \"two colon four eight\" or as a year.",
            "why": "The one thing no linter can check. axe proves a label exists and the snapshot proves it has not changed; only an ear can tell you the phrase is one a person would use, and only a real voice engine tells you how Arabic-Indic digits actually come out.",
            "shot": "ayah-selected"
          },
          {
            "do": "Double-tap an ayah with hops, open a rail chip, and try to escape the sheet: swipe past the last item, and swipe backwards past the first. Then hop to a target and immediately swipe once.",
            "expect": "Both swipes stay inside the sheet — neither walks you into the page behind it. After the hop, the next swipe lands on something real on the new page, not on a node from the sheet that no longer exists.",
            "why": "Focus containment and focus survival across a page change are the two failures that make hopping unusable without sight, and neither is visible to a runner: the tree is correct in both the working and the broken case. The sheet's own contents — «إغلاق», «انتقل إلى …» — are pinned in e2e/__aria__/*/hop-popover.aria.yml and are not what you are checking here.",
            "shot": "hop-popover"
          },
          {
            "do": "Now do the whole tour with the screen off or your eyes closed: find an ayah, hop, come back along the trail.",
            "expect": "You can do it. Whatever you could not do — or could only do because you already knew where things were — is the result of this check.",
            "why": "Every label can be correct and the app still unusable, because you knew where to swipe. This is the actual verdict; the two steps before it are how you find out why it failed.",
            "shot": "trail-beads"
          }
        ],
        "reading": [
          "A label that reads awkwardly, or digits your voice speaks wrongly → change the label in packages/core/src/highlighter.ts (enhancePolygons) or the component that owns it, then regenerate the aria snapshots (`pnpm -C apps/web exec playwright test share-a11y --update-snapshots`) and read the diff — it is the new announcement, written down.",
          "Focus escaping a sheet, or stranded after a hop → an e2e assertion, not a label change. The tree is already correct in both cases, which is exactly why this half stayed human.",
          "A control announced in the wrong order or not at all → that is a snapshot failure and should have failed CI before you picked up the phone. If it did not, the surface is missing from the aria-snapshot set — add it to apps/web/e2e/share-a11y.spec.ts.",
          "A tour you could complete but found exhausting → write that in `result` anyway. Feel is not a bug report, but it is exactly what this check can see and axe cannot.",
          "Nothing wrong at all → say which reader, which OS version, and which phone. A pass on VoiceOver 17 is not a pass on TalkBack."
        ],
        "record": "make record CHECK=screen-reader-walkthrough RESULT='VoiceOver iOS 17 on iPhone SE — full tour by gesture; ayah labels read «الآية البقرة · ٢:٤٨» (fine); rail chips read as words; one finding: <…>'"
      },
      "tunes": [
        "apps/web/e2e/__aria__/ — the pinned trees. Delivered ahead of this check (2026-07-27); a phrasing verdict lands here as a regenerated snapshot, which is the announcement written down",
        "apps/web/e2e/share-a11y.spec.ts — a surface with no snapshot yet, or a focus behaviour you could break, becomes a test there",
        "packages/core/src/highlighter.ts — the aria-label shape, if the labels read badly"
      ],
      "record": "docs/decisions/loop-3.md §a11y"
    },
    {
      "id": "kfgqpc-terms-primary-source",
      "title": "Read the Complex's published terms at the source",
      "why": "SOURCES.md currently reads KFGQPC's terms through quran-svg's NOTICE.md, which quotes them verbatim and cites Royal Decrees 136/8 and 9/B/46356 — strong, but secondary. The app states those terms to every reader in the colophon, and an earlier paraphrase of them was wrong.",
      "how": "Open qurancomplex.gov.sa in an ordinary browser. The host is unreachable from automation, and this was measured rather than assumed: a GitHub-hosted runner (Azure westus2, run 30285643491, 2026-07-27) resolves the A record to 66.9.131.70 and then times out on TCP 443 — connect rc=124, and curl returns 000 under both a curl and a desktop-Chrome User-Agent. The same failure appears from the maintainer's machine. Two unrelated networks failing at the TCP layer is not a bot filter a header could talk past, and web.archive.org is not fetchable from the tooling either, so no scheduled watcher can do this. It stays a human check permanently. Confirm the wording still says free digital/web/software use with commercial printing reserved.",
      "owner": "user",
      "status": "pending",
      "blocks": [],
      "runbook": {
        "needs": [
          "An ordinary browser, outside this environment — ideally one on a residential connection, since two datacentre/office networks have now failed to reach the host. It resolves and then times out on TCP 443 from both a GitHub runner in Azure westus2 and the maintainer's machine, and the Wayback mirror is not fetchable from the tooling either. That is the entire reason this is a human check, and the reason no watcher will ever be built for it.",
          "About five minutes."
        ],
        "setup": [],
        "steps": [
          {
            "do": "Open https://qurancomplex.gov.sa/en/ and find the terms / digital publications page.",
            "expect": "It loads. (It does not load from this repo's tooling, which is why nobody has read it yet.)",
            "why": "The primary source, not a mirror. Every licensing claim this project makes about the glyph corpus currently rests on a third party's summary, and a summary is precisely where a restriction gets softened."
          },
          {
            "do": "Read what it says about reusing the digital mushaf.",
            "expect": "Free use in digital, web and software contexts, with **commercial printing of physical masahif** reserved to the Complex — the same restriction quran-svg's NOTICE.md quotes, citing Royal Decrees 136/8 and 9/B/46356.",
            "why": "What is at stake is whether the reserved right is commercial printing of physical masahif or something broader. Those two readings put this project in very different places."
          },
          {
            "do": "Compare that against the two places we restate it: SOURCES.md §hafs-kfqc, and the colophon in the app (tap the wordmark حِفظ → «الرخصة والمصدر»).",
            "expect": "Both should describe free digital use with the printing restriction — and specifically NOT «إتاحة حرّة للاستعمال غير التجاري» (non-commercial only), which is a different edition's term and shipped once as a live defect.",
            "why": "We restate these terms in two places a stranger can read, so a wrong restatement is a licensing claim we are making on the Complex's behalf. This exact string was wrong once and shipped.",
            "shot": "colophon-source"
          }
        ],
        "reading": [
          "Wording matches what we say → drop the 'pending that glance' clause from SOURCES.md's Status line. Nothing in the build changes.",
          "Wording has moved → change SOURCES.md AND apps/web/src/components/Colophon.tsx together; they are the same claim in two places.",
          "You are unsure whether our summary is stricter than the source → treat that as a failure and write it down. Overstating someone else's terms fails silently: it reads as caution, so no reader ever files a bug about being told they have fewer rights than they do.",
          "The Libyan Endowments editions (Qālūn/Warsh) really are non-commercial-only — do not let their terms drift back onto this row."
        ],
        "record": "make record CHECK=kfgqpc-terms-primary-source RESULT='read at qurancomplex.gov.sa/en/ on <date> — free digital use, commercial printing reserved; matches SOURCES.md and the colophon'"
      },
      "tunes": [
        "SOURCES.md §hafs-kfqc — drops the 'pending-that-glance' clause from the Status line",
        "apps/web/src/components/Colophon.tsx — the mushaf licence line, if the wording moved"
      ],
      "record": "SOURCES.md §hafs-kfqc"
    },
    {
      "id": "source-offer-resolves",
      "title": "The GPL §6 offer resolves for a stranger",
      "why": "Publishing a static PWA conveys it: the browser receives real copies of the JS and of assets/roots/**, a GPL-covered derivative. The colophon links SOURCE_REPO at the exact build commit, which discharges the obligation only if that URL opens for someone who is not signed in as us.",
      "how": "Open the colophon's source link in a private/logged-out browser window against the deployed build. It must land on real source, at a commit, not a 404.",
      "owner": "user",
      "status": "pending",
      "blocks": [
        "public-deploy"
      ],
      "runbook": {
        "needs": [
          "Two minutes of network. `make source-offer` follows the same links anonymously and answers the machine half before you pick up a phone.",
          "The **deployed** build, not localhost. The question is what a stranger receives, and localhost cannot answer it.",
          "A private / logged-out browser window — signed in as ourselves, a private repo looks public."
        ],
        "setup": [
          {
            "run": "make source-offer URL=<the deployed URL>",
            "expect": "A verdict, printed. `source-offer — OK` → every link already resolves anonymously and what is left below is confirming the *reader’s* path to them. `DOES NOT RESOLVE` → it names which link failed and why; you can stop here and fix that. `COULD NOT TELL` → the network would not answer, which is not a pass."
          }
        ],
        "steps": [
          {
            "do": "Open the deployed site in a private window.",
            "expect": "The app loads normally.",
            "why": "Private, because your own session hides the failure that matters: a link that resolves only because you are signed in does not resolve for the stranger the GPL offer is written for. `make source-offer` has already followed the links with no credentials at all; what it cannot tell you is whether a reader can *get to* them from inside the running app, which is what the rest of this is for."
          },
          {
            "do": "Tap the wordmark حِفظ at the top of the chrome (it announces «عن حِفظ»).",
            "expect": "The colophon sheet opens, with a «الرخصة والمصدر» section and a link labelled «الشيفرة المصدرية».",
            "why": "The offer has to be reachable from inside the running app, not just from the repository. A colophon nobody can find is an offer that is not being made.",
            "shot": "colophon-source"
          },
          {
            "do": "Note the short commit printed beside the link, then follow the link.",
            "expect": "Real source at that exact commit — a `/tree/<sha>` URL — not a 404, not a sign-in wall, and not a bare branch that moves under the reader. The sha is baked at build time (CF_PAGES_COMMIT_SHA → GITHUB_SHA → git rev-parse HEAD).",
            "why": "A bare branch link moves under the reader. The offer is for the source of the binary they are running, and the pinned sha is what makes it a real offer rather than a gesture."
          },
          {
            "do": "Scroll the colophon to the credits and follow one of the attribution links too.",
            "expect": "They resolve. The mutashabihat licence asks for a mention in the app; corpus.quran.com's terms require the link.",
            "why": "Two of the corpora we build on ask for a link, not merely a mention. A dead attribution link is a licence term being quietly failed.",
            "shot": "colophon-credits"
          }
        ],
        "reading": [
          "404 or a sign-in wall → the §6 offer is not discharged and the site must not be published. Either make the repo public, or repoint SOURCE_REPO in apps/web/src/provenance.ts at wherever the source is actually served.",
          "Lands on the repository root with no commit → the build had no sha available. The offer is weaker but not broken; fix the build environment, not the constant.",
          "Lands on a commit that is not the deployed build → the deploy and the link disagree, which is worse than no link. Check the build-time resolution order.",
          "`make source-offer URL=<deployed>` disagrees with what you see on screen → believe the script about the links and your eyes about the path to them. It reads SOURCE_REPO and the build’s commit out of the deployed bundle, so a disagreement means the page you opened and the bundle it loaded are not the same deploy."
        ],
        "record": "make record CHECK=source-offer-resolves RESULT='private window → colophon → الشيفرة المصدرية resolves to <repo>/tree/<sha>, matching the deployed commit'"
      },
      "tunes": [
        "apps/web/src/provenance.ts — SOURCE_REPO, if the source is served from somewhere other than the GitHub repo",
        "scripts/check-source-offer.mjs — if the offer moved, or a new credited source needs its link followed"
      ],
      "record": "docs/PLAN.md follow-up ⑤"
    },
    {
      "id": "edge-spot-audit",
      "title": "Edge spot-audit — 20 sampled pairs against a printed mushaf",
      "why": "The data is scripture. Every automated gate here proves the shards are well-formed; none can prove an edge is true. This is the only check that can, and it is the one whose results are worth the most per minute spent.",
      "how": "Draw 20 pairs from the shipped shards — stratified so every kind of edge gets looked at, not just the common kind — look each up in a printed mushaf, and judge whether they are genuinely mutashabihat. Record every verdict — correct AND wrong — in packages/etl/data/qa/verified-edges.json.",
      "owner": "user",
      "status": "pending",
      "blocks": [
        "web-v1.0"
      ],
      "staleAfterDays": 14,
      "runbook": {
        "needs": [
          "A printed mushaf — the KFGQPC Hafs print the pages are photographed from, so page numbers line up.",
          "30–45 minutes for twenty edges. A hafiz if you can get one; this is the check where a second reader is worth the most.",
          "Nothing else — the draw runs here."
        ],
        "setup": [
          {
            "run": "make validate",
            "expect": "Ends with an edge-audit coverage table: one row per class of edge (type × curated-or-dataset × page distance), each marked ✓ or ·. The · rows are the classes nobody has ever looked at — those are what this round is for."
          },
          {
            "run": "make audit-edges N=20 SEED=1",
            "expect": "A header naming the edition, the seed, and the rerun command, then twenty numbered **pairs** — `from → to  type  p.<page> / p.<page>  curated|dataset  words <n>  roots <n>/<n>` — then a JSON block ready to paste. Use a fresh SEED per round; add NEW=1 to draw only from pairs no verdict has settled."
          }
        ],
        "steps": [
          {
            "do": "Work through the twenty pairs in the print, one at a time.",
            "expect": "Each `from` and `to` is findable at the page printed beside it. A pair you cannot locate is itself a finding — record it as wrong with a note.",
            "why": "This is the only check in the project that can say the data is true. Every other tier verifies that the app faithfully renders whatever the ETL produced — including when what it produced is wrong."
          },
          {
            "do": "Read `words` and `roots` beside each pair as hints, and distrust them.",
            "expect": "`words` is the longest run of words the two ayahs share — the same score gate:edges enforces a floor on, computed by the same reader (packages/etl/scripts/morphology.mjs), so a number here can never disagree with CI. `roots` is how many of the shorter ayah's roots appear in both, in order.",
            "why": "They point at the pairs worth slowing down on from both ends: a high `words` with a wrong verdict is a plausible-looking false edge, and a low one that is nonetheless right is the case the product exists for — 2:48 → 2:123 swaps شفاعة and عدل, scores 1, and is genuinely confused by huffaz every day."
          },
          {
            "do": "For each, answer the question the tool prints — not «are these similar?» but «**would a hafiz confuse these two while reciting?**»",
            "expect": "Most edges are obvious either way in a few seconds. The slow ones are the informative ones; note why.",
            "why": "\"Similar\" is not the standard; a hafiz's confusion is. An edge that is textually close but never actually confused is noise in the rail, and noise is what makes a reader stop trusting the hop."
          },
          {
            "do": "Optionally open a pair in the app to see what a reader actually sees: make phone, then navigate to the ayah and open its rail.",
            "expect": "The edge appears as a hop under the direction chip its `type` implies.",
            "why": "Catches a correct edge with a wrong presentation. A real mutashabih filed under the wrong direction chip sends the reader looking in the wrong surah, which reads as bad data even though the data is fine.",
            "shot": "hop-rail"
          },
          {
            "do": "Paste the printed JSON block into packages/etl/data/qa/verified-edges.json under `entries`, setting each `verdict` to `correct` or `wrong`, `verifiedBy` to your name, and `note` where a future reader needs the reason.",
            "expect": "Forty entries for twenty pairs — the block writes out both directions of each pair, because every edge in the corpus is generated in both and one reading settles both.",
            "why": "Recording the `correct` verdicts matters as much as the failures: they are what turns half an hour of reading into a permanent regression fixture instead of an anecdote."
          },
          {
            "do": "Run pnpm gate:verified-edges.",
            "expect": "OK. Those verdicts are now enforced in both directions, forever — a `correct` edge that disappears and a `wrong` edge that returns both fail the build.",
            "why": "This is the step where manual work becomes automated. Once it is green, a `correct` edge that disappears and a `wrong` edge that returns both fail the build — forever, without anyone re-reading twenty pairs."
          }
        ],
        "reading": [
          "Record BOTH verdicts. A rejected edge that is not written down comes back on the next data refresh and costs the reader's time all over again — and no automated check can tell a wrong edge from a right one.",
          "If several wrong edges share a shape — same `type`, same phrase length, same surah pair — that is a filter in packages/etl/scripts/build-adjacency.mjs, not twenty more rejections. A class of error is worth more than the twenty.",
          "If gate:verified-edges fails later, do not edit the fixture to go green: the data moved under a human verdict. Find out what moved it, and re-verify with a reader before changing a verdict.",
          "This check goes stale after 14 days because the data keeps moving. A fresh seed each round means the coverage accumulates instead of re-checking the same twenty.",
          "Aim at the · rows. The draw is stratified for a reason: 97% of the corpus is one class (dataset mutashabih, far apart), so a flat draw of twenty essentially never shows a shared-root edge, a related-meaning edge, or a pair we curated ourselves — and those are exactly the classes no automated check covers, since gate:edges deliberately does not score shared-root at all. Pass UNIFORM=1 only when the overall *rate* of bad edges is the question, and note that gate:edges already carries the rate on every commit."
        ],
        "record": "make record CHECK=edge-spot-audit RESULT='seed 1 · 20 edges · 18 correct, 2 wrong (both phrase-overlap between 2 and 3) → verified-edges.json'"
      },
      "tunes": [
        "packages/etl/data/qa/verified-edges.json — every verdict becomes a permanent gate assertion via gate:verified-edges",
        "packages/etl/scripts/build-adjacency.mjs — a systematic class of wrong edge becomes a filter, not 20 more rejections"
      ],
      "record": "packages/etl/data/qa/verified-edges.json"
    },
    {
      "id": "offline-survival-8-day",
      "title": "8+ day offline survival, installed vs browser tab",
      "why": "Safari's ITP deletes all script storage for origins untouched for 7 days of browser use, and Home-Screen-installed apps are exempt. That single asymmetry is why the install prompt is a feature and not a nicety — and it has never been observed, only read about.",
      "how": "Pin a juz in both an installed PWA and a Safari tab. Leave both for 8+ days of normal browser use. Then go into airplane mode and try to revise the pinned juz in each.",
      "owner": "user",
      "status": "pending",
      "blocks": [
        "loop-6b"
      ],
      "runbook": {
        "needs": [
          "**Not runnable yet.** Pin-a-juz ships in Loop 6b; today there is nothing to pin. What exists now — visited pages surviving a reload offline, and the app recovering from an eviction — is already asserted in apps/web/e2e/offline.spec.ts. The steps below are written so that when 6b lands, the only cost left is the waiting.",
          "**What the eight days no longer have to find.** Research ④ replaced the *eviction* half of this check with `Storage.clearDataForOrigin` over CDP, which takes the origin's caches out from under a live page exactly as a sweep does. That found two real defects (the precache never rebuilt after eviction, so one sweep ended offline support permanently; and a page that failed to fetch left the chrome claiming a page the stage was not showing). Both are fixed and asserted in the `Hifth · eviction` describe. So this check is now only about the thing no harness can supply: whether iOS actually sweeps an uninstalled origin at ~7 days and spares an installed one.",
          "An iPhone (the ITP asymmetry is Safari's), and a deployed build.",
          "Eight days of ordinary phone use. The clock counts days of *browser use*, not calendar days — a phone left in a drawer does not run this experiment."
        ],
        "setup": [],
        "steps": [
          {
            "do": "On the phone, open the deployed site in a Safari tab and pin a juz.",
            "expect": "The pack downloads and the juz is marked available offline.",
            "why": "The tab is the case we expect to lose. Proving it worked on day 0 is what makes a day-8 failure evidence rather than a guess."
          },
          {
            "do": "Add the site to the Home Screen, open it from there, and pin the same juz again inside the installed app.",
            "expect": "Two independent storage buckets now hold the same pack. That is the experiment.",
            "why": "Two buckets, same pack, same phone, same eight days. It is the only arrangement that lets you attribute a loss to the storage sweep rather than to the network, the build, or the device."
          },
          {
            "do": "Write down today's date, then use the phone normally for 8+ days. Do not open Hifth in either place.",
            "expect": "Nothing. This step is the check.",
            "why": "ITP counts seven days of browser use, not seven days of wall clock, and it cannot be fast-forwarded. The waiting is the instrument."
          },
          {
            "do": "On day 8 or later, turn on airplane mode and open the Safari tab: try to revise the pinned juz — open pages, hop, turn pages.",
            "expect": "The tab may have lost everything (ITP clears script storage for origins untouched for 7 days of browser use). If it has, the app should notice and offer to re-pin rather than showing broken pages.",
            "why": "Airplane mode is what makes this a storage test rather than a cache test: whatever still works is genuinely resident on the device."
          },
          {
            "do": "Still in airplane mode, open the installed app and do the same.",
            "expect": "The juz is still there. Installed apps are exempt from that sweep — this asymmetry is the whole reason the install prompt is first-class.",
            "why": "If the installed app survives and the tab does not, the install prompt stops being a nicety and becomes the feature that makes offline revision real — which is what Loop 6b has to be designed around."
          }
        ],
        "reading": [
          "Tab evicted, install survived → the assumption holds. Assert the eviction-detection path in apps/web/e2e/offline.spec.ts and keep the install prompt prominent.",
          "Both survived → note the exact iOS version; the rule may have relaxed. The install prompt's copy then becomes a nudge rather than a warning, which is a copy change, not an architecture one.",
          "The installed app was evicted too → the re-pin offer's trigger in apps/web/src/storage.ts is now urgent rather than theoretical, and the offline story needs rethinking before any 'works offline' claim ships.",
          "Anything showed a broken page instead of an offer to re-pin → that is the bug, whichever bucket it happened in."
        ],
        "record": "make record CHECK=offline-survival-8-day RESULT='iOS <ver>, pinned <date>, checked <date+8> — tab: <evicted|intact>, installed: <evicted|intact>; re-pin offer <appeared|did not>'"
      },
      "tunes": [
        "apps/web/e2e/offline.spec.ts — the `Hifth · eviction` describe already asserts recovery against a simulated sweep; tune it to what the phone actually did (which bucket lost what, and whether the shell repair fired before the reader noticed)",
        "apps/web/src/pwa.ts — `repairShellCache()` re-registers `sw.js` under a query-suffixed URL, because a script URL the registration has never seen is the only thing observed to force the install that refills an evicted precache. If iOS evicts differently — registration gone too, or storage back but stale — that trigger is what changes.",
        "apps/web/src/storage.ts — the re-pin offer's trigger, if eviction looks different than assumed"
      ],
      "record": "docs/decisions/loop-6b.md"
    }
  ]
}
